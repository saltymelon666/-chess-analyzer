# 棋盘研究所项目状态

更新时间：2026-09-07

## 51. 2026-09-13 棋书式实战着因果与结果正文

- “实战着分析”的核心正文不再强制复述棋子起止格、保护关系、控制格数量，也移除了“先看全局”“实战把选择摆上棋盘”“再看关键选择”“留给初学者的原则”等报告式标题。正文现在按棋书的论证顺序直接写：走前局面判断、实战着结论、对手利用机制、强制变化及最终兑现结果。
- 新增程序验证的后果命题：对次佳着可合法重放紧接着的连续吃子，逐步说明谁吃谁、是否回吃、净损失多少，以及该收益在完整路线中是否保留；覆盖多步交换与一步直接丢后/丢子。对好棋只允许把从实战着开始、没有被非吃子着打断的强制交换写成该着收益，禁止把PV后段碰巧发生的吃子倒推为首着作用。
- 对等价换后、连续交换后物质持平、暂时吃子随后被追回的变化，会明确说明“不是直接丢后/丢子”，并把评价差限定在交换后的局面；已有完整后果命题时不再重复插入“关键转折”免责声明。棋书知识库继续负责选择分析重点和表达顺序，不把其他棋局的具体棋子、格子或结论移植到当前局面。
- 专业缓存版本升至 `professional-v42-book-consequence`。专项回归最终 `127 passed`；完整回归 `385 passed, 3 failed, 3 warnings`，3项仍为既有公测文案、`chess.js`加载方式和开局上下文断言漂移，本轮没有新增失败。
- 完整15局真实 Stockfish + DeepSeek 最终代码前一版为首次通过 `13/15`、最终严格通过 `15/15`、安全回退 `0/15`，核心命题逐字落地 `46/46`、人工金标关键原子召回 `89.7%`，见 `work/book-style-practical-v48/`。最终交换边界针对 `center-2`、`simplify-2` 再跑为首次及最终严格通过 `2/2`、安全回退 `0/2`，见 `work/book-style-practical-v50-targeted/`；随后仅删除已有后果时重复的首应答句，已由专项与完整回归覆盖。自动门禁不等同于专家盲审，最终自由文风仍需用户验收。

## 50. 2026-09-13 实战着分析结论回归栏目

- “实战着分析”现在对每个已分析实战着展示，不再只限于疑问着或失误；结论直接写入该栏目，明确说明走法质量及围绕同一局面矛盾的具体原因，因此好棋也会说明好在哪里。
- Stockfish验证路线继续单独展示；PV末端的“轮到白方/黑方走”只表示搜索截断点，不再冒充“最终战术结果”。只有路线确实验证到重大子力得失时，才显示“路线验证到的结果”。
- 页面会兼容过滤旧缓存中的截断状态文案；事实、颜色、路线和引用校验均未放松，专业提示词也未改动。根目录与 `docs/index.html` 同步。
- 专业分析与前端相关回归在排除一个既有开局回退链断言后为 `90 passed`，新增两项定向测试 `2 passed`；完整回归 `382 passed, 3 failed, 3 warnings`，3项失败仍为既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移。两份页面内联JavaScript语法、SHA-256一致性与 `git diff --check` 均通过。

## 49. 2026-09-07 棋书叙述路径与文风规则

- 专业提示词升至 `professional-v39-book-narrative-path`，`BookEvaluationStyle` 升至1.2：核心正文按“局面矛盾 → 选择与代价 → 棋理启示”组织，统一只讲一个重点，要求术语紧接具体解释、比喻克制、变化证明因果，不输出内部思考过程或固定三段标题。
- `DecisionContext` 升至1.2：相同首着不自我比较，小评价差不强制写惩罚，近期偏离首选不自动证明当前选择没有解决局面问题。原有事实、颜色、路线及引用校验不变；多步后果继续在对应路线区保留中间条件。
- 修复用户可见文本先去重、后替换“实战着”导致 `e3e3` 等重复SAN的问题：改为先替换标签再去重，结构化走法不变。新增小差距历史干扰、提示词路径传递及SAN替换回归覆盖。未修改前端，根目录与 `docs/index.html` 哈希一致。
- 完整15局真实 Stockfish + DeepSeek首次通过12/15、最终自动严格校验15/15、安全回退3/15，原子引用精度100%、人工金标关键原子召回95.6%；3局回退涉及接口传输中断。最终代码补跑6局，首次通过5/6、最终自动严格校验6/6、回退0/6，引用精度和金标召回100%。分别保存于 `work/book-narrative-v39/results.json` 与 `recheck-results.json`，不得合并宣称一次整套零回退。
- 自动通过不代表文风或因果关系全部合格：王攻补跑中“h4为白马跳到d4或e7提供支撑”的解释缺少已核实的因果证据，部分开头仍模板化，战术正文仍可能由事实保护程序重建。详见 `work/book-narrative-v39/review-notes.md`；自由文本语义与用户盲审仍待验收，未为提高通过率放松门禁。
- 修改后分析、决策上下文、质量集及API专项115 passed；最终完整回归362 passed、3 failed、3 warnings，相比修改前360 passed、3 failed新增2项测试通过，3项失败仍为公测文案、chess.js加载方式、开局上下文前端断言漂移。Python语法及 `git diff --check` 通过，暂存区为空。本轮未提交、推送或部署。

## 48. 2026-09-04 三子将杀精确局面扩库

- 统一棋书数据库新增 Project Gutenberg 公版来源 William Brett Fishburne《Checkmates for Three Pieces》。原文中的580个FEN逐条通过`python-chess`合法性、三子数量和将杀状态校验后，以`exact_position`写入；条目只确认完全相同局面的将杀事实，不附会最佳着、评价、计划或相似局面结论。
- `scripts/expand_unified_book_knowledge.py`新增可重复执行的三子将杀抽取与幂等写入流程，来源记录、知识记录和FTS5索引同步更新。数据库由9,456条、23个来源扩充至10,036条、24个来源，FTS记录同为10,036；二次运行后数量不变，`PRAGMA integrity_check`为`ok`且无外键错误。
- 来源登记修正Hoyle与Foster已入研究库的过期状态，并登记三子/四子将杀数据集。四子版原文约18.8MB，条目规模远大于当前运行库，本轮只标记为已评估并暂缓，不做任意抽样或直接扩大部署体积。
- 新增三子将杀接受/拒绝测试，并与统一棋书检索一起回归，共`9 passed`；精确局面查询可返回新增来源及其严格权限边界。完整回归为`354 passed, 3 failed, 3 warnings`，3项失败仍为既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移，本轮没有新增失败。

## 47. 2026-09-03 v30棋书式讲解盲审页

- 基于 `work/core-book-v30/results.json` 生成15局盲审页 `work/human-review-v30-core-book/boards.html`；`king-attack-3` 使用单独复跑后严格通过且无安全回退的版本。题目按固定种子打乱，只显示匿名编号、棋盘、实战着、引擎首选、当前用户可见核心段落和前六个半回合的核对变化，不显示题型、模型状态、回退、内部引用、自动指标或原始局面ID。
- 评分直接对应本轮三个目标：重点是否清楚准确、原因是否具体易懂、读完是否知道相似局面先检查什么；每项独立选择“是/否”，支持修改意见、本机自动保存和JSON导出。匿名编号与原始局面的映射单独保存在 `manifest.json`，不进入盲审页面。
- 新增可重复生成脚本 `scripts/build_core_book_blind_review.py` 和3项专项测试；页面内联JavaScript语法通过。桌面渲染为双列，真实390px设备尺寸模拟为单列，`clientWidth=390`、`scrollWidth=390`，无横向溢出。完整回归 `350 passed, 3 failed, 3 warnings`，3项仍为既有公测文案、`chess.js` 加载方式和开局上下文前端断言漂移，本轮没有新增失败。

## 46. 2026-09-03 围绕核心痛点的棋书式讲解

- 专业分析提示词进一步收紧为单一讲解主线：第一句直接给出 `corePainPoint` 的核心判断，随后说明实战着意图、具体机制和可验证后果；短变化只用于证明文字，不能代替中文解释。修正了旧规则要求局面摘要同时覆盖子力、中心和两翼的冲突，不再为了显得全面罗列全部评价维度。
- `BookEvaluationStyle` 升至 `1.1`，主要重点上限改为1，并加入三项读者自检：能否一句话复述重点、能否说明原因、能否知道相似局面下一次先检查什么；可迁移提醒只能归纳当前已验证机制，不能增加事实。专业分析缓存版本升至 `professional-v30-core-book-explanation`。
- 前端“先说结论”按“程序判断 → 走法意图/机制 → 具体问题 → 首选路线为何更好”的顺序合成一个自然段，并过滤“不是大错但另一着更精确”等与首句重复的同义判断。根目录与 `docs/` 页面保持同步。
- 质量评测补充接受当前程序控制的等价判断措辞，如“就是引擎首选”“另一着更精确”和“差约若干兵”；只调整文本匹配，不改变证据集合、引用路径或事实门禁。完整15局真实 Stockfish + DeepSeek 套件为首次通过 `14/15`、最终严格通过 `15/15`、安全回退 `1/15`、引用精度 `100%`、人工金标关键原子召回 `97.1%`。唯一回退局面 `king-attack-3` 随后单独复跑，重试后严格通过且无安全回退；结果保存在 `work/core-book-v30/`。
- 完整回归 `347 passed, 3 failed, 3 warnings`。3项失败仍为既有公测文案、`chess.js` 加载方式和开局上下文前端断言漂移，本轮没有新增失败；两个页面的内联 JavaScript 语法与内容同步检查通过，桌面和390px手机宽度的本地HTTP渲染未见横向溢出或结构异常。

## 45. 2026-08-31 第三轮人工反馈复核

- 读取用户导出的第三轮审核结果：7题中4题通过，`tactic-2`、`king-attack-3`、`simplify-2` 共3题需改；裁决归档于 `work/human-review-v25-round3/user-review-adjudication.json`。
- `tactic-2` 接受语言意见，把“净收两个兵值”改为“取得约两分子力的交换收益”，仍明确这只是开头连续交换的可验证收支，不冒充最终物质结论。
- `simplify-2` 接受遗漏意见。新增程序事实提取：当对手在候选路线中推进某兵、随后该兵被当前方明确吃掉时，最终路线会直接说明对应推进着和吃兵着；本题现在写明 `...b5` 后白方以 `Qxb5` 吃掉该兵。
- `king-attack-3` 的“制造叠兵”与验证路线不符：`h4、Rd7、hxg5、hxg5` 后黑方只有一个g5兵。审核页改为说明黑方h兵被迫回吃到g5、王翼兵形改变，并明确没有形成叠兵；没有为了迎合意见放松事实校验。
- 专业分析和质量集回归 `89 passed`；新增两项程序事实专项 `3 passed`。三个反馈局面均完成真实DeepSeek复跑并最终严格通过、无安全回退，其中战术题在调整受保护措辞后再次首次通过。审核页改用新的本机保存键，重新打开时不会沿用上一轮的“需改”选择。

## 44. 2026-08-31 v25第三轮正式人工审核

- DeepSeek额度恢复后正式重跑7个局面：`opening-1`、`opening-3`、`tactic-2`、`king-attack-3`、`center-1`、`closed-1`、`simplify-2`。首次校验通过 `7/7`、最终严格通过 `7/7`、安全回退 `0/7`，引用精度和人工金标召回均为 `100%`；结果保存于 `work/human-review-v25-round3/results-funded.json` 和 `report-funded.md`。
- 人工预检发现：当实战着本身就是Stockfish第一选择时，模型仍可能虚构两者差异；已经改为程序确定性说明“实战着就是第一路线首着”，不再让模型自我比较。另补强“为易位做准备”的同义表达保护，王已易位或没有对应易位权时不能通过严格校验。
- 修复从起始FEN精确查询开局时 `queryPly=0` 被模型约束拒绝的问题；完整PGN仍使用路径上下文确认开局，单独FEN不足以确认的深层变例不会冒充确定结论。修正后的3个相关局面再次真实运行，首次通过 `3/3`、安全回退 `0/3`。
- 新增第三轮简版人工审核页 `work/human-review-v25-round3/boards.html`，共7题，支持“通过/需修改”、本机自动保存和JSON导出；桌面两列、手机单列均已渲染检查。终审后状态栏明确区分“统一棋书数据库已接入”“最终严格事实校验7/7通过”和引用/金标指标；当前仍为本地审核页，尚未部署到线上网站。
- 统一棋书、决策上下文、开局、专业分析、API和真实质量集专项回归 `156 passed`；完整回归 `338 passed, 3 failed, 3 warnings`，3项仍为既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移，本轮没有新增失败。相关Python文件编译和 `git diff --check` 通过。

## 43. 2026-08-30 v25第三轮试跑受外部额度阻断

- 计划重跑4个第二轮修改局面并补齐3个尚未审核局面，共7题：`opening-1`、`opening-3`、`tactic-2`、`king-attack-3`、`center-1`、`closed-1`、`simplify-2`。DeepSeek对7次请求均返回HTTP 402，结果全部进入程序安全回退；保存于 `work/human-review-v25-round3/`，仅可证明严格事实链路 `7/7` 有效，不能作为v25人类语言质量结论，因此没有继续生成第三轮人工审核页。
- 安全回退已确认两项程序改动生效：`tactic-2` 使用“算完这串连续吃子，眼前净收两个兵值”的短句；`simplify-2` 明确“评价相差约0.85兵，有精度差距，但不是决定性失误”。
- 质量脚本现在与生产API一致，会把 `MoveReview.openingContext` 传入专业分析服务。固定质量集从中盘FEN直接起步时没有可确认的完整开局路径，因此不能借此冒充开局命名测试；生产完整PGN路径和单元测试仍覆盖程序确认开局名称的最终保留。
- 开局与质量相关回归 `19 passed`。下一步必须在DeepSeek额度恢复后重跑这7题，只有无安全回退的新输出才进入第三轮人工审核。

## 42. 2026-08-30 第二轮人工审核裁决与v25校准

- 第二轮8题审核结果归档至 `work/human-review-v24-round2/results.json`：`tactic-3`、`king-attack-1`、`center-2`、`closed-2` 通过；其余4题提供修改意见。原导出中带文字意见但未点裁决的项目按需修改处理，无法由程序事实确认的意见单列待确认。
- `opening-3` 暴露出提示词虽要求介绍开局名称，但硬事实后处理会覆盖模型局面摘要。现在 `OpeningPresentation.displayName` 和程序确认的开局简介由后端在最终摘要中强制保留，模型和清理流程都不能把名称再次丢掉。
- `tactic-2` 的连续交换账目仍由程序计算，但用户可见句子由“阶段性、交换段收支”改为“算完这串连续吃子，眼前净收……”，并明确后续未走完，不把临时账目冒充最终子力结论。提示词同时禁止常见报告腔套话。
- `simplify-2` 的意见为“两着差得不多”。当实战着与首选不一致但评价损失小于50厘兵时，最终摘要明确为“差距很小”；50—100厘兵时给出差值并说明是精度差而非决定性失误，避免用评价方向跨过零点夸大实际差距。
- `king-attack-3` 的“制造孤兵”未直接写入：验证路线为 `h4、Rd7、hxg5、hxg5`，最终黑方g兵仍有f兵位于相邻兵线，不符合程序的孤兵事实，需人工进一步明确所指棋子后再决定是否属于别的兵形概念。
- 专业分析缓存版本升至 `professional-v25-reviewed-coach-language`。新增开局名称最终保留、交换短句和小评价差批评边界测试；相关专项 `107 passed`。完整回归 `336 passed, 3 failed, 3 warnings`，3项仍为既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移，本轮没有新增失败。

## 41. 2026-08-30 第二轮简版人工审核

- 现场复核统一棋书数据库文件存在且可读，大小约30MB，包含 `9,456` 条知识记录、`23` 个来源，全文索引记录数同为 `9,456`；API中的 `unified_book_knowledge` 与默认仓库解析到同一数据库路径，质量脚本也显式注入同一仓库。
- 新选取8个未进入第一轮审核的局面：`opening-3`、`tactic-2`、`tactic-3`、`king-attack-1`、`king-attack-3`、`center-2`、`closed-2`、`simplify-2`。逐局调用生产检索接口验证为 `8/8` 命中，每局获得1—2条结构类比或棋书原则上下文；没有把类比来源当作当前棋盘事实。
- 简版审核页保存在 `work/human-review-v24-round2/boards.html`，每题只显示棋盘、实战着、Stockfish首选、局面判断和一句痛点解释；支持“通过/需改”、本机自动保存和JSON导出。页面内容来自最近一次无回退的v23严格结果，经当前v24事实边界重新压缩；由于DeepSeek仍返回HTTP 402，它不是新的v24模型重跑结果。
- 审核页内联JavaScript语法通过；桌面和窄屏布局已渲染检查。Impeccable检测器因缺少HTML解析依赖使用降级正则模式，未报告机械问题。

## 40. 2026-08-30 人工审核反馈校准

- 第一轮6局面人工审核中，开局、中心、封闭结构和简化局面基本通过；直接战术局面指出“没有明确说明连续交换可净得一个兵值”，王攻局面指出“把黑方原本已有的子力优势误写成该路线新获得的优势”。本轮只针对这两个可复现问题和开局表达做最小修正。
- 候选路线现在由程序按连续吃子逐步计算阶段性子力收支；只有路线从首着开始的连续交换、每个被吃棋子和净收益都可验证时，才写明哪一方阶段性净得多少兵值，并明确后续变化仍可能改变差额。模型自行写出的“获得、取得、形成、建立或扩大物质优势”全部按未经验证的子力收益整句移除，避免把局面基线误说成路线结果。
- 已确认开局上下文存在时，提示词要求先交代开局名称和常见战略方向，并承认开局通常有多种合理选择；除非程序质量与评价损失明确证明是失误，不把Stockfish首选包装成唯一正确的开局原则。专业分析缓存版本升至 `professional-v24-human-material-ledger`。
- 新增阶段性交换账目、注入位置、物质优势同义表达拦截及开局表达测试。专业分析相关专项 `105 passed`；完整回归 `334 passed, 3 failed, 3 warnings`，3项仍为既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移，本轮没有新增失败。
- `opening-2`、`tactic-1`、`king-attack-2` 真实试跑保存在 `work/human-review-v24-pilot/`。DeepSeek当前连续返回HTTP 402，因此3局均使用严格通过的程序安全回退，不能视为新的模型质量结论；安全版已确认 `tactic-1` 明确写出白方阶段性净得一个兵值，`king-attack-2` 不再出现“通过路线获得物质优势”的错误句。完整15局真实DeepSeek套件待外部额度恢复后重跑。当前仍未部署。

## 39. 2026-08-30 棋书相关性收紧与人工审核样本

- 统一棋书精确局面检索不再只按FEN和记录编号任取前三条；生产调用会同时传入实战着与Stockfish首选着，只保留与当前决策着法相符的原评，并限制同一来源和精确原评数量，为结构类比或原则说明保留上下文空间。相似局面画像新增兵的推进区间和中心兵精确位置，降低“兵在相同文件但结构完全不同”的误匹配。棋书上下文版本升至 `1.2`。
- 决策趋势仍可表示同一方近期多次偏离首选，但不再把中间出现过最佳着的情况写成“连续偏离”；决策上下文版本升至 `1.1`。专业分析缓存版本升至 `professional-v23-human-readable-context`。
- 真实样本复核发现严格事实校验此前仍允许 `black_black_king`、`white方` 和 `Ignore Test` 等内部术语进入用户可见中文。本轮将程序事实描述统一转换为“黑王、白方、应对验证”等自然中文，并新增最终响应拦截，内部棋子变量或内部测试术语再次出现时不能通过严格校验。
- 完整15局面真实Stockfish + DeepSeek套件保存在 `work/relevant-book-context-v23-final/`：首次通过 `14/15`、最终严格通过 `15/15`、安全回退 `0/15`、引用精度 `100%`、内部术语泄漏 `0/15`、人工金标召回 `97.1%`。`king-attack-3` 首次草稿因越界事件声明重试后通过；旧金标的 `tactic-3` 首要回应和 `king-attack-2` e6计划仍是两项未命中，没有放松事实校验追求100%。
- 从开局、直接战术、王攻、中心、封闭结构和简化局面各选一题，生成 `work/human-review-v23-final/manual-review.md`。审核稿隐藏内部引用ID，保留FEN、实战着、首选着、三条路线和最终讲解，并明确标出“通过程序校验但仍可能棋理过度”的句子供人工判断；当前尚未部署。
- 完整回归为 `331 passed, 3 failed, 3 warnings`；3项仍为既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移，本轮没有新增失败。

## 37. 2026-08-30 整局决策上下文与核心痛点选择器

- 专业分析新增程序控制的 `DecisionContext`：API从当前着之前最多5个半回合提取同一方的走法质量、评价损失区间和是否命中Stockfish首选，不发送历史FEN，也不让DeepSeek猜测棋手心理。程序据当前首要任务、当前评价损失和近期决策信号选择一个 `corePainPoint`，要求输出先解释痛点，再比较实战着与首选路线如何处理同一问题。
- 历史趋势只允许 `first_decision / stable / repeated_non_best / repeated_large_loss` 四种受控结论；“连续偏离首选”不自动升级为“反复犯同一种棋理错误”。首步或缓存中没有历史时正常降级为当前局面目标。专业分析缓存版本升至 `professional-v21-decision-context`，缓存键纳入最近5步的走法、首选、质量和损失，避免相同局面在不同对局历史下误用旧讲解。
- 复核发现此前质量脚本创建的独立 `ProfessionalAnalysisService` 没有注入统一棋书库；本轮已修正脚本并重新运行，因此 `work/decision-context-v21-final/` 才是同时覆盖“统一棋书上下文 + 痛点选择器”的正式真实结果。此前v20报告只能证明API接线和原分析链路稳定，不能单独证明质量脚本使用了棋书库。
- 新增决策趋势、首步降级、5步上限、Prompt注入、API历史传递和缓存隔离测试；相关专项 `98 passed`。15局真实Stockfish + DeepSeek套件为首次通过 `15/15`、最终严格通过 `15/15`、安全回退 `0/15`、引用精度 `100%`；通用安全回退短语由上一批46处降至42处，整句删除/重建由86处降至82处。金标召回仍为 `97.1%`，两项旧金标与当前稳定引擎路线/计划共识不一致，没有修改事实或放松校验追求100%。
- 完整回归为 `326 passed, 3 failed, 3 warnings`；3项失败仍是既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移，本轮没有新增失败。15局质量集每局只有一个待分析半回合，因此验证了当前痛点选择和棋书上下文，但没有覆盖真实长棋局中的重复趋势；该部分已经有程序/API回归，仍待新增真实多回合盲评样本。

## 36. 2026-08-28 统一棋书知识库接入专业分析

- 专业分析正式接入随应用部署的 `app/data/unified-book-knowledge.sqlite3`，不再停留在“数据库已构建但主分析链路未调用”的状态。检索顺序为完全相同合法状态的棋书原评优先，其次从经典棋手已验证局面中选择结构相似原评，章节原则只在通过旧记谱、OCR残段和主题前段匹配过滤后使用。
- 棋书上下文最多3段、单段最多700字符；传给DeepSeek时不包含来源FEN。完全相同局面仍须由当前事实包复核具体着法和评价，相似局面与原则摘录只能迁移观察顺序、因果解释和棋书语言，禁止迁移来源棋子、格子、走法、胜负、主动权、威胁或计划。数据库缺失或没有合格结果时静默降级，不影响Stockfish分析。
- 专业分析缓存版本升至 `professional-v20-unified-book-context`，并纳入棋书上下文协议版本，避免旧缓存继续返回未使用知识库的结果。新增统一库精确匹配、原则过滤、无库降级和Prompt实际注入测试。
- 专项回归 `94 passed`。15局真实Stockfish + DeepSeek质量套件为首次通过 `15/15`、最终严格通过 `15/15`、安全回退 `0/15`、引用精度 `100%`；通用安全回退短语由旧基线55处降至46处，报告中的整句删除/重建记录由192处降至86处。报告保存在忽略目录 `work/unified-book-context-v20/`。完整回归为 `318 passed, 3 failed, 3 warnings`，3项失败仍是既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移，本轮没有新增失败。
- 当前金标召回为 `97.1%`：`tactic-3` 当前稳定首选回应已与旧金标不同，`king-attack-2` 的旧计划在当前路线共识下不再生成；两项均保留为评测基线待复核，没有修改事实或放松校验来追求100%。本阶段完成的是知识检索接线和安全边界，不代表人类教练式分析已经完成，后续仍需增加真实棋手盲评与整局决策上下文。

## 35. 2026-08-28 PostgreSQL Analytics 初始化修复

- 线上运营后台出现“后台统计暂不可用”时，确认 Analytics 存储没有完成初始化；棋局分析服务和健康检查仍正常，但访问统计、分析日志和用户反馈会被静默跳过写入。
- 根因是反馈字段已经包含在 `CREATE TABLE` 中，初始化随后仍执行普通 `ALTER TABLE ... ADD COLUMN`。PostgreSQL 遇到重复列后会把当前事务标记为失败，即使 Python 捕获重复列异常，后续语句仍会失败并使 `analytics_store` 回落为 `None`。
- PostgreSQL 迁移改用原生 `ADD COLUMN IF NOT EXISTS`，旧数据库可补列、新数据库和已迁移数据库均不会触发事务失败；SQLite 继续保留原有兼容迁移路径。
- PostgreSQL 可移植性回归测试新增三个反馈字段的幂等迁移断言。Analytics/API 专项测试 `21 passed`；完整测试 `315 passed, 3 failed, 3 warnings`，3 项失败仍为既有公测文案、`chess.js` 加载方式和开局上下文前端断言漂移，本轮没有新增失败。
- 修复代码尚待部署并重启 Render 服务；部署后必须用有效 `ADMIN_STATISTICS_KEY` 验证 `/api/admin/dashboard`、提交一条实际反馈并确认后台可读取，不能仅以 `/api/health` 返回 200 作为 Analytics 恢复依据。

## 34. 2026-08-27 复盘专用局面评价条与引擎路线面板

- 主界面保持原样；只有整盘 Stockfish 分析成功并进入逐步复盘状态后，棋盘右侧才显示局面评价条，右侧控制区才显示当前局面评价、分析深度和最多三条已验证路线。评价条不显示“黑/白”端点文字，分数使用轻量标记；引擎评价区不显示重复的分析状态小字。清空棋谱或重新导入未分析棋谱后，两处组件恢复隐藏。
- 2026-08-28 根据界面反馈移除引擎评价区“已完成分析 · 跟随复盘更新”提示，并移除评价条两端“黑/白”可见标签；无障碍评价说明继续保留在 `aria-label` 中。
- 复盘进度条、前后步按钮和着法列表继续使用现有 `currentStep` 状态；切换局面时直接读取整盘分析已经返回的走前/走后评价、`candidate_lines` 和终局 `actual_move_line`，不新增 DeepSeek 棋盘事实来源，也不额外启动浏览器持续引擎计算。
- 开发版 `index.html` 与发布版 `docs/index.html` 已同步；两个页面内联 JavaScript 语法通过，复盘评价条专项测试 `4 passed`。Edge 无头浏览器确认未分析时组件隐藏，评价条位于棋盘右侧，分析后桌面显示三条路线，390×844 手机与桌面均无横向溢出或页面脚本错误；本地未启动后端时仅有预期的健康检查连接失败。
- 完整 pytest 为 `315 passed, 3 failed, 3 warnings`；3 项失败仍为既有公测文案、`chess.js` 加载方式和开局上下文前端断言漂移，本轮没有新增失败。Impeccable 检测器在依赖缺失的降级模式下仅报告页面既有装饰告警，本轮新增的评价条高度动画已按检测结果移除。

## 33. 2026-08-26 Stockfish 低深度路线稳定性保护

- 用户棋谱在 `10.Ne5` 后的局面暴露了固定深度 10 的主变波动：整盘顺序搜索曾暂时把黑方 `10...Nxd4` 评为 `-0.91`，但深度 12 以上首选翻转为 `10...Bb7`，深度 22—24 收敛至约 `0.00`。该问题来自低深度 MultiPV 未收敛，不是 DeepSeek 改写分数。
- 整盘分析现在对首选/次选分差至少 50cp、相邻局面评价跳变至少 50cp 或涉及将杀的低深度结果自动复核至至少深度 16；若首选或评价仍发生实质变化，再确认至至少深度 20。普通稳定局面继续保留快速初筛。
- 引擎路线排名改为读取 Stockfish 返回的 `multipv`，排序后强制要求 `rank=1` 存在；首选路线校验失败时明确终止，禁止把第二路线静默升级为最佳路线。
- 用户完整 98 半回合棋谱通过真实 Stockfish 回归：`10.Ne5` 前后评价为约 `-0.04 → -0.17`，黑方首选与候选路线第一项均为 `10...Bb7`，整盘耗时约 15.7 秒。引擎、复盘与事实包专项测试 `20 passed`；完整回归 `311 passed, 3 failed, 3 warnings`，3 项失败仍为既有公测文案、`chess.js` 加载方式和开局上下文前端断言漂移。

## 32. 2026-08-26 高置信计划原子与程序目标保护

- `commentary-gold-v3` 在v2的45个核心原子和6个战术细节原子上，新增17个计划原子。计划金标只来自 `StrategicPlanAnalyzer` 在至少两条已验证Stockfish路线和结构事实中识别出的 `high` 置信度计划；没有程序计划证据的 `tactic-3`、`center-2`、封闭中心和简化局面继续隐藏计划，不用类别级期望或空泛文字强行补齐。
- 原子报告增加按 `verdict / mechanism / opponent_resource / consequence / practical_plan` 类型统计，并单列已验证计划召回。v3第一轮真实基线为计划 `15/17`（88.2%）：`opening-1` 的“角落马”属于金标同义表达遗漏；`tactic-1` 的具体“集中攻击c6弱兵”被模型泛化为“注意防守资源和反击”，属于真实内容退化。
- 高置信计划说明现在始终由程序验证的 `plan.goal` 开头，DeepSeek解释只能追加、不能替换具体目标；安全清理后只剩“继续比较路线”时也回落到程序目标。缓存版本升至 `professional-v19-program-owned-plan-goal`。
- 最终15局真实Stockfish + DeepSeek复跑：首次通过 `15/15`、最终严格通过 `15/15`、安全回退 `0/15`、引用精度 `100%`、核心原子 `45/45`、细节原子 `23/23`、已验证计划原子 `17/17`；报告保存在忽略目录 `work/commentary-gold-v3-v19-final/`。专项回归 `85 passed`；完整回归 `309 passed, 3 failed, 3 warnings`，3项仍为既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移。

## 31. 2026-08-25 专业分析原子质量集完整覆盖

- `commentary-gold-v2` 将原子评测扩展到全部15个真实质量局面：每局固定标注实战着评价、Stockfish首选首着和首要回应3个核心原子，共45个；另在4个有明确 `python-chess` 战术事实的局面标注6个细节原子。核心与细节召回分开汇总，避免用浅层路线完整性掩盖棋理机制缺失。
- 第一轮真实Stockfish + DeepSeek基线为核心原子 `37/45`（82.2%）、细节原子 `5/6`（83.3%）。复审确认7个核心漏失是金标未接受“首选不一致”这一等价表达，已补充同义表达；其余真实缺陷为 `opening-3` 的程序评价理由被长度压缩清空，以及 `simplify-1` 的双攻正文没有携带对应战术事实ID。
- 长度压缩现在永久保留程序控制的实战着评价；后端注入经验证战术句时同步合并对应 `positionFacts.threats` 事实ID。专业分析缓存版本升至 `professional-v17-atomic-evidence-coverage`，避免继续返回旧证据链结果。
- 最终15局真实复跑：首次通过 `15/15`、最终严格通过 `15/15`、安全回退 `0/15`、原子事实引用精度 `100%`、核心原子召回 `45/45`、细节原子召回 `6/6`，报告保存在忽略目录 `work/commentary-gold-v2-final/`。专项回归 `82 passed`；完整回归 `306 passed, 3 failed, 3 warnings`，3项仍为既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移。

## 30. 2026-08-24 专业分析原子质量评测骨架

- 新增独立的棋局讲解原子质量层，把最终结构化输出拆成“判断、机制、对手资源、后果、实际计划”五类可审计主张；引用合法率与专家关键要点召回率分开计算，不再把结构校验通过率等同于内容质量。
- 真实质量套件支持每个局面提供人工审核的 `goldAtoms`，并按原子类型和可接受证据集合计算必需要点覆盖；未提供人工金标准时报告明确显示“未计算”，不会从程序标签自动伪造专家答案。
- 修正首要任务顺序：根节点着法形成且经 `python-chess` 验证的双攻、牵制、串击或战术性牺牲，现在优先于泛化的物质/活动动态平衡；候选路线内部战术仍不能升级为当前威胁。专业分析缓存版本升至 `professional-v16-current-tactic-priority`。
- `tactic-2`、`king-attack-2`、`simplify-1` 三个回归局面的首要任务均稳定为 `forcing_tactics`；其中前两局修改前曾被错误归为 `dynamic_balance`。真实Stockfish + DeepSeek试跑为首次通过 `3/3`、最终严格通过 `3/3`、安全回退 `0/3`、原子引用精度 `100%`，完整输出保存在忽略目录 `work/objective-priority-pilot/`。
- 原子匹配进一步限定输出字段、合法证据集合与必需语义词组，避免“引用相同但没有讲到同一意思”造成虚高；JSON数组下标按字面匹配，仅 `[*]` 表示任意下标。
- 新增独立的 `commentary-gold-v1` 人工规则金标：不读取待评价的DeepSeek正文，仅依据Stockfish首选路线、`python-chess`验证事实和固定结构，为 `tactic-2`、`king-attack-2`、`simplify-1` 标注12个必讲原子。它是3/15局面的校准集，不冒充外部棋手专家认证或完整15局覆盖。
- 最终真实Stockfish + DeepSeek复跑为首次通过 `3/3`、最终严格通过 `3/3`、安全回退 `0/3`、原子引用精度 `100%`、人工金标关键原子召回 `12/12`；报告保存在忽略目录 `work/commentary-gold-v1/`。原子化、专业分析与质量集专项 `79 passed`；完整回归 `303 passed, 3 failed, 3 warnings`，3项失败仍是既有公测文案、`chess.js`加载方式和开局上下文前端断言漂移。完整30题旧人工审核对应的局面库仍缺失，不能据此推断未标注局面的内容召回。

## 29. 2026-08-21 专业分析战术事实文字复审

- 复审用户反馈的“立即以象赢得中心兵”后确认根因：旧后处理会把模型自由文本中的“吃掉”机械替换为“赢得”，导致未经对应走法验证的吃子结论换词后绕过事件校验。
- 删除该危险换词路径；模型自由文本中的吃子、将军、将杀及赢兵/子力收益声明现在按完整句删除，同一字段内其余完整句保留。具体战术事件继续只由后端根据对应 Stockfish ply 与 `python-chess` 事实生成。
- 增加解析后防线：未经前置归一化的草稿不能直接进入解析；最终响应若仍含“以象赢得中心兵”等物质收益声明也会被严格校验拒绝。专业分析缓存版本升至 `professional-v15-strict-event-claims`，旧结果不再命中新缓存。
- 专业分析、质量集与 API 专项测试 `87 passed`。最终代码的 15 局面真实 Stockfish + DeepSeek 套件为首次通过 `15/15`、最终严格通过 `15/15`、重试 `0/15`、安全回退 `0/15`、三条路线有效 `15/15`，目标错误短语命中为 0；结果保存在忽略目录 `work/strict-event-claims-final/`。
- 完整 pytest 为 `298 passed, 3 failed, 3 warnings`；3 项失败仍是当前工作区既有的公测文字、`chess.js` 延迟加载和开局上下文前端断言漂移，本轮没有新增失败。

## 28. 2026-08-21 独立复习训练页面

- 分析页不再内嵌“我的复习”列表或走棋训练；顶部增加“复习训练”入口，错误局面的“加入复习”保存成功后变为“开始复习”，再次点击携带局面 ID 进入独立 `review.html`。
- 新复习页继续使用浏览器本地 `pawnlab_review_positions_v1` 数据和现有棋盘、棋子资产，不新增后端事实来源；队列中不显示推荐着，只有用户在棋盘上走出 Stockfish 推荐着后才展示正确答案。2026-08-21 根据页面反馈暂时移除“查看原局复盘说明”及对应展示代码。
- 非最佳合法着会立即撤回并要求继续尝试，非法着也不会结束题目；答对后可选择“还不熟 / 基本掌握 / 已经掌握”，分别安排 1 / 3 / 7 天后复习，并自动进入下一道到期题目。
- 训练棋盘按当前行棋方自动调整方向，黑方题目黑方在下；棋盘提供 64 个可聚焦格子按钮、方向键漫游、选中格与合法目标提示，并保留桌面和手机响应式布局。升变题必须由用户明确选择后、车、象或马后才核对完整 UCI，不自动代选；移除复习记录后可撤销。
- `index.html` / `docs/index.html` 与 `review.html` / `docs/review.html` 已分别同步；新增产品约束记录 `PRODUCT.md`，明确中国用户、网站优先、分析与训练分层及棋盘事实来源边界。
- 四个页面的内联 JavaScript 语法通过；复习专项断言 `1 passed`。Edge 无头浏览器完成首页待复习数量、错误着撤回、正确着通过、白题切换黑题、黑方翻转、32 枚棋子、64 个交互格、方向键焦点、后升变错误后撤回、马升变正确通过、移除撤销、390×844 手机无横向溢出和页面脚本错误检查，结果全部通过。
- 最新完整 pytest 为 `296 passed, 3 failed, 3 warnings`；3 项失败均为当前工作区既有前端断言漂移，本轮复习专项没有新增失败。Impeccable 检测器在依赖缺失的降级模式下报告复习页既有阴影样式告警，本轮删除说明没有新增视觉结构。

## 27. 2026-08-21 关键局面本地复习

- 保持现有首页、棋盘、逐步复盘和“小兵研究员说”结构不变，只在 `?!`、`?`、`??` 错误着法中增加“加入复习”；复习事实使用整盘分析已经返回的走前 FEN、实战着和 Stockfish 推荐着，不新增 DeepSeek 棋盘事实来源。
- 复习记录以带版本的数据结构保存在浏览器 `localStorage`，最多保存 50 个局面；没有记录时整个“我的复习”区域隐藏，已有记录可查看来源、错误等级、评价损失、推荐着和下次复习时间。
- 用户可从复习列表进入原局面，在现有棋盘上点击棋子与目标格完成合法走棋；作答后比较用户选择、Stockfish 推荐和原实战着，并可选择“还不熟 / 基本掌握 / 已经掌握”，分别安排 1 / 3 / 7 天后复习。
- 开发版与发布版页面保持一致；两个内联 JavaScript 语法通过。Edge 无头浏览器完成保存、刷新持久化、棋盘正确着 `g8-f6`、掌握度更新和 390×844 手机无横向溢出检查，页面脚本错误为 0。
- 新增复习前端断言通过；完整 pytest 为 `293 passed, 3 failed, 3 warnings`，3 项失败均为当前工作区既有前端断言漂移，与本轮复习功能无关。Impeccable 检测器在依赖缺失的降级模式下报告 3 项既有装饰样式告警，本轮新增组件没有命中告警。

## 26. 2026-08-20 棋盘棋子清晰度升级

- 保留现有 2048×2048 写实棋盘底图、固定白方朝向、坐标、高亮、箭头和 PGN 走子交互，只替换棋子视觉资产。
- 新棋子采用暖象牙白与深青墨色、双向高对比描边和饱满 Staunton 轮廓，针对网页缩放与手机尺寸提高辨识度，并与页面米白、薄荷绿、黄色视觉体系保持一致。
- 通过 ImageGen 生成透明 PNG 母版，拆分为 12 个通用棋子后转换为透明 WebP；开发版与发布版分别保存于 `assets/chess/pieces-v2/` 和 `docs/assets/chess/pieces-v2/`，并保留 `piece_contact_sheet_v2.webp` 作为设计母版。
- 页面棋盘底图改为引用既有 `physical_chess_board_empty.webp`，棋子映射改为外部资源路径，移除棋盘与棋子的内嵌 base64，降低 HTML 体积并便于后续维护。
- 新棋子资产专项测试 `1 passed`；Edge 桌面 1440×1100 与手机 390×844 实际检查均加载 32 枚棋子，手机端导入 6 个半回合 PGN 后仍正常显示全部棋子和 6 个走法按钮，无控制台错误、资源错误或横向溢出。完整 pytest 为 `292 passed, 3 failed, 3 warnings`，3 个失败与改动前状态文档记录的既有前端断言漂移一致，本轮未新增失败。
- 2026-08-21 根据页面复审将整套棋子重制为严格的二维平面图形：每枚棋子只保留暖象牙白、深青墨色和透明像素，不含高光、渐变、投影、倒角或材质纹理；开发版与发布版继续使用原有资源路径，不影响棋盘和走子逻辑。
- 平面版在 Edge 桌面 1440×1100 与手机 390×844 中复测：32 枚棋子全部加载，导入 6 个半回合后显示 6 个走法按钮，棋子滤镜为 `none`，页面和棋盘均无横向溢出，页面脚本错误为 0。棋子资产专项测试 `1 passed`；完整 pytest 为 `291 passed, 5 failed, 3 warnings`，其中 2 项来自当前工作区开发页与发布页不同步，另外 3 项为既有旧前端断言，本轮平面素材替换未引入棋盘功能失败。
- 2026-08-21 进一步解决写实木纹棋盘与平面棋子风格不一致：新增严格按 `2048×2048`、四周 `164px`、单格 `215px` 绘制的 `flat_chess_board_v2.webp`，以暖米白、薄荷青绿、暖黄色和深青绿纯色构成，不含木纹、透视、反光或内部阴影；分析页和独立复习页的开发版、发布版均同步使用该棋盘，并统一深青绿坐标标记。
- Edge 桌面 1440×1100 与手机 390×844 导入 6 个半回合后均加载 32 枚棋子和 6 个走法按钮，新棋盘资源与深青坐标样式生效，棋子滤镜为 `none`，无页面脚本错误或横向溢出。棋盘相关专项测试 `3 passed`，完整 pytest `293 passed, 3 failed, 3 warnings`；3 项失败仍为既有公测文字、`chess.js` 加载方式和专业开局上下文断言漂移，本轮未新增失败。
- 2026-08-21 页面复审发现整格大小的棋子会贴近最外两排边界并与格内坐标标签重叠；棋子显示尺寸由单格 `215` 缩进为 `190`，中心坐标不变，字母与数字坐标移入棋盘外框区域。Edge 初始局面桌面与手机均确认 32 枚棋子完整位于棋盘范围内、16 个坐标不再遮挡棋子，无脚本错误或横向溢出。
- 2026-08-21 继续校正生成素材内部透明留白：旧素材的实际轮廓曾出现横向约 `-84px` 至 `+82px` 的偏移，外层图片居中仍会造成棋子视觉歪斜。新增 `pieces-v3/`，逐枚按非透明像素边界裁切并重新放回 `512×512` 画布中心，12 枚棋子的实际轮廓中心误差均不超过 `0.5px`；分析页和独立复习页已同步切换到新目录，`piece_contact_sheet_v3.webp` 保留为校正母版。
- Edge 桌面与手机初始局面均确认 32 枚 `pieces-v3` 棋子加载成功、每列棋子中心与对应棋格中心一致，无脚本错误或横向溢出。棋盘定位专项测试 `3 passed`，完整 pytest `296 passed, 3 failed, 3 warnings`；3 项仍为既有旧断言，本轮未新增失败。

## 25. 2026-08-19 反馈关联分析结果

- 首页反馈提交会在存在当前专业分析时，一并发送分析 ID、当前着法上下文和用户页面上可见的分析正文；未进行分析时仍可提交普通反馈。
- 运营后台新增独立“用户反馈”区域，用户评价、用户建议和对应分析结果分开显示，长分析结果按需展开。
- SQLite 与 PostgreSQL 事件表通过兼容迁移新增 `analysis_result` 字段；后台接口按所选日期返回最近反馈。
- 反馈相关单元、API 与前端断言通过；完整回归为 `291 passed, 3 failed`，3 项均为当前分支既有前端断言漂移，与反馈链路无关。
- Edge 桌面 1440×1100、手机 390×844 实际后台检查通过：用户评价、用户建议和分析正文均可见，无控制台错误、无横向溢出。
- 生产发布已完成：PR #23 于 2026-08-19 合并为 `74c9260`，GitHub Pages 构建成功；`pawnlab.cn` 首页与后台返回 200，Render 健康检查正常且 OpenAPI 已包含 `analysis_result`、`recent_feedback`，无密钥访问后台数据接口仍按预期返回 401。

本文档是继续开发时的首要状态来源。内容只依据当前代码、Git 状态、自动化测试、仓库内质量报告和部署配置；无法从这些材料确认的内容均标为“待确认”。

## 1. 当前快照

- 当前分支：`codex/latest-frontend`
- 当前提交：`91f9c67`
- 当前工作区不是干净状态，存在尚未提交的“完整删除关键棋子栏目”和Phase 6A修改。
- 根目录 `index.html` 与发布目录 `docs/index.html` 当前内容一致。
- “小兵研究员说”主模块固定保留并始终显示；前端展示“双方子力与局面”“白方计划/黑方计划”和Stockfish三条研究路线。路线的“第一步作用”由程序根据已验证首着生成，不再展示DeepSeek自由编写的直接目的；候选路线内部吃子/子力变化事件卡不展示。质量标记为`?!`、`?`或`??`的实战着会额外展示完整验证路线和最终战术结果，正常着法不展开。双方是否已经易位、易位权及易位历史相关句子不在局面描述中展示；棋书原评、弱点、潜在威胁和王的安全栏目仍不展示。
- 本地自动化测试：`291 passed, 2 warnings`（2026-08-06，路线首着作用硬事实保护后的完整回归）。
- 15局面真实质量套件：最终严格校验`15/15`，首次通过`14/15`，`center-1`因DeepSeek传输中断进入程序安全回退；三条路线有效`15/15`。
- Render 与 GitHub Pages 的当前线上运行状态：已验证。合并提交 `76d1ccc` 后，GitHub Pages 返回 200；Render `/api/health` 返回 200，Stockfish 可用、DeepSeek 已配置，开局与残局 lookup 接口均返回 200。

### 前端首次加载性能优化

- 101 张生产图片已从 PNG 转为 WebP，并同步到 `assets/` 与 `docs/assets/`；单份资源总量由 10.07 MiB 降至 2.05 MiB，减少 79.7%，当前最大的 WebP 为 489.4 KiB。
- `chess.js` 已改为用户首次导入 PGN 或分析局面时通过 `pgn-runtime.js` 动态加载；Stockfish WASM 保持按需路径，不进入首次页面请求。
- Fast 4G（1.6 Mbps、150 ms RTT）、冷缓存、桌面 1365×768、5 次中位数：首次传输 3811.1 KiB → 762.0 KiB，DOMContentLoaded 1330.1 ms → 740.2 ms，load 19795.2 ms → 4131.3 ms，LCP 17844 ms → 1980 ms。
- 桌面、平板、手机浏览器冒烟检查均通过：无横向溢出、无控制台错误，`chess.js` 首屏不加载，导入示例 PGN 后按需加载并正确恢复 12 着。
- 完整自动化回归：`337 passed, 2 warnings`；警告为既有 Starlette 废弃提示及本机 `.pytest_cache` 写权限提示。
- 15 局面真实 Stockfish + DeepSeek 质量套件：首次通过 `15/15`，最终严格通过 `15/15`，重试 `0/15`，安全回退 `0/15`；输入 Token `5,458—8,704`，首次网络响应 `12.041—16.611` 秒，缓存响应 `674ms`。结果写入被忽略的 `work/frontend-performance/`，未覆盖仓库历史质量报告。

### Phase 6D / 6E / 6F 最新进展

- 两轮人工棋书局面审核已经完成；正式样本库为30题，另有2题排除。
- 30题真实 Phase 6D 基线全部完成，运行错误0；基线程序主导主题识别为18/30。
- Phase 6E 已接入双攻、牵制、串击、战术性牺牲、第七横线活动和路线作用域信号；无DeepSeek结构回归为28/30。
- Professional安全模板从候选路线生成全局直接危险的旁路已经删除；相关基线问题由10题降为0。
- 8个受影响真实局面中，程序主题和Professional最终文本均为8/8；无证据主动权为0。
- Analysis Report 已读取 Position Interpretation；在同一批8题真实事实包上，程序安全报告主题命中由2/8提升到8/8。
- Phase 6F 已实现有界 Prepared Threat Ignore Test；CC-022 的 `Nd5` 经 3 个中性忽略着验证，最小评价损失为 2.38 兵。
- Phase 6F 已实现条件化杀王深度升级；30题中只有 BB-02 触发并确认 `Qf1+` 为 M3，没有新增将杀误报。
- 30题结构回归为30/30；30题真实全链完成30/30，运行错误0，无当前威胁作用域误写和无证据主动权。
- 当前工作已到人工审核节点：`docs/research/phase6f-final-human-review-boards.html` 用于审核主次、棋理与自然度。
- Phase 6E完整报告：`docs/research/chess-analysis-phase6e-validation-report.md`；Phase 6F完整报告：`docs/research/chess-analysis-phase6f-validation-report.md`。
- 当前样本只批准用于离线评测、回归与规则校准，不是DeepSeek训练数据，也没有进入生产Prompt。

### Phase 7A 棋书自动抽取与检索可行性

- 已完成30题逐题留一检索实验；查询侧不读取当前题的人类答案。普通混合检索Top-3主题族为63.3%，但跨棋书仅20.0%，低于跨书随机基线42.9%，当前30题不能直接作为生产RAG。
- 已在`work/research_books/`下载4本Project Gutenberg研究副本；该目录被`.gitignore`排除，不进入产品或提交范围。
- 已为《Chess and Checkers: The Way to Mastership》实现纯文本棋盘自动抽取：检测102个棋盘编号，75个恢复棋子，73个有效棋盘，42个同时具备有效棋盘、行棋方和相邻原始解释。
- 用现有12个CC正式局面做解析盲对照：棋盘12/12完全一致，行棋方12/12一致。
- 已为《The Blue Book of Chess》实现PGN原注释自动抽取：85盘有效棋局、228个去重合法注释局面，保存走前FEN、着法、走后FEN和原始注释；85个空解析分段被自动忽略，1盘带PGN错误并记录。
- 现有6个BB正式局面中4个可按走前棋盘和标注着精确找回；另2个因原书注释挂载边界不同而不强行匹配。
- 两本书合计得到约270个程序可恢复的原始棋书条目；其中只有具备明确题解范围和可验证主题的子集可进入检索，不能把数量直接当作270条标准答案。
- 把42个自动条目用于棋盘外形检索后，Top-3主题提示为90.0%，但随机基线已达86.3%，证明仅凭棋盘外形和原文常见词没有有效增益。
- Phase 7A结论：棋书自动抽取可行，当前检索不可接入生成链。下一步必须扩展不同作者的同主题覆盖，并为自动条目运行Stockfish、ThreatPackage与结构化评价来源后再测试。
- 报告：`docs/research/chess-analysis-phase7a-retrieval-feasibility-report.md`、`docs/research/phase7a-cc-extraction-validation-report.md`、`docs/research/phase7a-bb-extraction-validation-report.md`、`docs/research/phase7a-book-corpus-retrieval-report.md`。

### Phase 7B / 7C 棋书数据库与棋理规则

- Phase 7B 已把三本完成棋盘恢复的公版棋书整理为本地SQLite研究库：共326题、323个不同棋盘；CC 42题、BB 228题、CF 56题。数据库位于被忽略的`work/research_books/phase7b-book-corpus.sqlite3`，不会随代码提交。
- 已登记8本公版候选棋书；另外5本因图片棋盘、旧记谱法或来源筛选尚未达到可靠自动恢复标准，没有为了扩充数量强行导入。
- Phase 7C 新增92条证据有界棋理规则：44条棋盘精确规则、22条棋盘启发式规则、12条Stockfish路线规则、14条MultiPV规则。
- 326题FEN离线标注共生成12,116条信号，命中54种规则；依赖路线或MultiPV的规则在没有引擎证据时保持不命中。
- 30题跨棋书盲检中，棋盘外形、规则、固定混合检索的Top-1主题族分别为53.3%、30.0%、33.3%；随机基线为37.3%。规则Top-3为73.3%，只比随机71.0%高2.4个百分点。
- 已新增第一版`PositionFactorRanker`，按规则可靠度、作用域和语料出现率把每题压缩为最多2个主导因素和3个辅助因素；326题平均输出4.98个因素。
- 显著性规则检索Top-1提升到46.7%，但仍低于棋盘外形53.3%；显著性混合为36.7%。当前仍否决接入DeepSeek。
- 真正缺少的部分已经收窄为Stockfish评价敏感度与因果验证：需要证明某因素是否导致评价变化、是否在多条路线中迫使对手应对，再更新重要性分数。
- 报告：`docs/research/chess-analysis-phase7b-book-corpus-report.md`、`docs/research/chess-analysis-phase7c-reasoning-rules-report.md`；数据：`docs/research/phase7c-book-rule-enrichment-summary.json`、`docs/research/phase7c-rule-retrieval-blind-results.json`。

### Phase 7D Stockfish评价敏感度与因果验证

- 已新增`EvaluationSensitivityAnalyzer`，为排序因素附加首选路线支持、稳定MultiPV覆盖、评价敏感度和`confirmed/supporting/unproven`状态；MultiPV分差不会被直接解释为静态主题的因果证明。
- 326个棋书局面使用Stockfish Depth 8、Threads 1、Hash 32 MB、MultiPV 3完成离线标注：错误0，威胁53个、计划323个、引擎支持规则信号372个。
- 1,629个候选因素中确认42个、支持59个、未证明1,528个；多数普通信号没有被升级为因果结论。
- 为消除低深度哈希状态影响，语料和查询都改成每个局面独立启动引擎；30题盲检连续两次指标完全一致。
- 最终因果检索Top-1/Top-3为40.0%/73.3%，随机为37.3%/71.0%，棋盘外形为53.3%/70.0%；固定棋盘+因果混合为33.3%/70.0%。生产门禁仍不通过。
- 已生成10题最小人工审核页`docs/research/phase7d-causal-human-review-boards.html`，用于区分自动粗标签错误与检索真实错误。完成这10题前不继续调权重，也不接入DeepSeek。
- 审核页中的30段候选棋书评注、主题名称、因果状态和书名信息已默认中文显示；英文原文折叠保留用于溯源。
- 项目负责人已完成10题人工审核：8题“有帮助”、2题“部分有帮助”、0题“无关”。这10题在自动精确主题族指标中全部属于Top-1失败，证明该指标存在严重假阴性，不能再作为主要检索成功标准。
- 审核纠正两处内容边界：`CF-AUTO-21`旧式记谱应译为`Bxf7+`；`BB-AUTO-G014-P043`的`...Bd6+`属于假设变化内部事件，不是根局面立即合法着。中文页已经修正并明确作用域。
- 审核结果归档为`docs/research/phase7d-causal-human-review-results.json`，裁决为`docs/research/phase7d-human-review-adjudication.json`。下一步主要评价目标改为“可迁移思路有用性”，精确主题族仅作诊断。
- 报告：`docs/research/chess-analysis-phase7d-evaluation-sensitivity-report.md`；原始结果：`docs/research/phase7d-engine-evidence-summary.json`、`docs/research/phase7d-causal-retrieval-blind-results.json`。

### Phase 7E 棋书上下文安全迁移 A/B

- 已新增棋书案例安全迁移包：只允许 DeepSeek借用类比局面的观察角度和思考顺序，禁止迁移源局面的棋子、格子、着法、评价、结果、威胁和主动权。
- `ProfessionalAnalysisService`和`NarrativeGenerator`只增加可选棋书上下文参数；现有调用默认不传，当前生产API行为不变。
- 原有硬事实、路线、颜色、引用和主动权门禁全部保留；书例ID不得成为当前局面证据引用。
- 选取6个未参与Phase 7D人工审核的留出局面，使用相同事实包、相同模型和温度完成真实DeepSeek A/B。两组均为4题通过校验、2题安全回退，运行错误0。
- 已生成3:3平衡且隐藏答案的可视棋盘盲审页`docs/research/phase7e-deepseek-book-ab-review-boards.html`。人工答案未进入Prompt；当前停在6题人工审核门禁，完成前不接入生产链。
- Phase 7E人工盲审已完成：基线胜1、棋书上下文胜0、相同3、两边都不安全2；平均棋理相关性同为2.33，棋书上下文自然度由3.00降至2.33。生产门禁未通过，棋书上下文保持关闭。
- 原始结果：`docs/research/phase7e-deepseek-book-ab-results.json`；人工裁决：`docs/research/phase7e-deepseek-book-ab-adjudication.json`；阶段报告：`docs/research/chess-analysis-phase7e-book-context-ab-report.md`。

### Phase 7F 分析目标选择

- `PositionInterpretationPackage`已增加程序控制的局面阶段和首要分析目标，覆盖胜势兑现、攻势兑现、残局计划、动态平衡、强制战术、着法质量解释和普通战略改善。
- 王位置、易位权和易位历史未知不再自动成为王安全战略主题；候选路线内部战术不再自动成为当前评价来源。
- 6个人工反馈局面的结构目标已经分别收敛为：CS-01胜势兑现、CS-04残局计划、BB-04强制战术、CC-029攻势兑现、CF-03着法质量解释、CF-06动态平衡。
- 同6题真实DeepSeek A/B已完成：修改前2题安全回退，修改后3题安全回退。自动观察存在局部改善但回退增加。
- 本轮没有使用棋书上下文，人工答案没有进入Prompt，生产API尚未启用新棋书检索。
- 项目负责人随后终止这类逐题主观A/B审核；Phase 7F页面保留为研究证据，但不再等待人工导出，也不作为生产门禁。

### Phase 7G 棋书原局面真值层

- 项目负责人决定：棋书原文直接作为其完全相同源局面的标准人类解释，不再进行逐题主观A/B审核。
- 新增`BookGroundTruthRepository`；精确匹配同时要求棋子摆放、行棋方、易位权和吃过路兵状态一致，半回合与完整回合计数忽略。
- 正式真值集包含326条原评、325个不同完整局面状态：BB 228、CC 42、CF 56；157,536个原评字符。
- 228条书中标注着全部通过python-chess合法性验证，非法标注着0条。
- 相似局面迁移被明确关闭；无精确命中时返回空结果，不把相似棋书原文当成正确答案。
- 56条CF数据保留`legal_board_fen_audit_pending`来源状态；按当前决策进入标准集，但不伪装成已人工逐图核验。
- 真值数据：`docs/research/phase7g-book-ground-truth-dataset.json`；清单：`docs/research/phase7g-book-ground-truth-manifest.json`；报告：`docs/research/chess-analysis-phase7g-book-ground-truth-report.md`。
- 当前只建立了可追溯真值层，尚未提交、部署或接入生产API。

### Phase 7H 棋书真值自动基准与最小接入

- 326条棋书原评精确查回为326/326；228条书中标注着全部合法。
- 程序任一候选因素覆盖为101/112（90.18%），但第一主题仅54/112（48.21%），首要分析目标52/112（46.43%）；当前真正薄弱点是重要性排序。
- `/api/professional-analysis`已增加`bookReferences`，仅在棋子、行棋方、易位权和吃过路兵状态完全一致时返回棋书原评。
- 前端将“棋书原评（精确局面）”优先显示；DeepSeek不读取或重写原文，DeepSeek失败也不影响已命中原评。
- 研究SQLite缺失时自动读取正式Phase 7G JSON真值集；未命中仍返回空结果，不进行相似局面迁移。
- 完整测试：`262 passed, 2 warnings`；未提交、未推送、未部署。
- 报告：`docs/research/chess-analysis-phase7h-book-ground-truth-benchmark-report.md`；结果：`docs/research/phase7h-book-ground-truth-benchmark-results.json`、`docs/research/phase7h-book-ground-truth-benchmark-summary.json`。

### Phase 7I 公版棋书全自动扩容

- 通过Classic Chess公开annotated-book API导入9本公版经典棋书，处理457盘带注释棋局。
- 新增4,695条原始棋书评注，下载错误0，SAN/UCI、行棋方、回合定位和python-chess合法性错误0。
- 正式真值集从326条扩大到5,021条，覆盖4,848个完整局面状态，原评总字符数920,603。
- 运行时已切换到`phase7i-book-ground-truth-dataset.json`，研究SQLite与正式JSON合并去重，并建立完整局面键索引。
- 棋书原文仍不进入DeepSeek Prompt，不迁移到相似局面，不覆盖python-chess和Stockfish事实。
- 完整测试：`267 passed, 2 warnings`；页面内联JavaScript语法通过，`git diff --check`无内容错误。
- 报告：`docs/research/chess-analysis-phase7i-public-domain-book-expansion-report.md`；清单：`docs/research/phase7i-book-ground-truth-manifest.json`。

### Phase 8A 开局路径与残局来源扩展

- 已从 Lichess `chess-openings` CC0 数据建立独立开局路径目录：3,810 条全部通过 `python-chess` 逐着合法性验证，拒绝 0 条；覆盖 500 个 ECO 编码、149 个开局家族，最长 36 个半回合。
- 每条开局记录保留完整体系名、变例层级、PGN、SAN、UCI、终点 FEN、完整局面键、父分支和转置关联字段；DeepSeek 不参与开局命名和走法恢复。
- 当前 5,021 条棋书原评中按现有阶段规则归为残局的只有 542 条，七子及以下只有 38 条，残局覆盖确实不足。
- 已审计并下载研究副本：`Chess Openings Ancient and Modern`、`Synopsis of Chess Openings`、`Chess Studies; or, Endings of Games`、`Chess: Theory and Practice`；均为旧式记谱或图像棋盘，尚未通过 FEN/行棋方/解答合法性门禁，不强行进入正式真值库。
- Project Gutenberg 三子与四子将杀数据分别有 580 和 551,739 个合法 FEN，但全部为已经将杀的终局，只适合终局识别测试，不作为解释知识数量灌入。
- 开局目录仍是离线知识数据，尚未接入生产 API、前端或 DeepSeek Prompt；残局下一步需要独立真值结构并用 Syzygy 校验七子及以下结论。
- 产物：`docs/research/phase8a-opening-path-catalog.json`、`docs/research/phase8a-opening-path-manifest.json`、`docs/research/chess-analysis-phase8a-opening-endgame-corpus-report.md`。

### Phase 8B 开局只读识别层

- 新增 `OpeningKnowledgeRepository` 与 `POST /api/opening-lookup`，支持使用 PGN、精确 FEN 或二者一致性校验查询开局身份。
- 返回 ECO、完整体系名、家族、变例路径、标准 PGN/SAN/UCI、匹配深度和后续命名分支；匹配类型区分完全路线、最长命名前缀、转置局面、精确 FEN和未命中。
- PGN 主线和每一步合法性由 `python-chess` 验证；PGN终点与同时提交的FEN不一致时返回422。
- 开局接口不调用Stockfish或DeepSeek，不判断路线好坏，不把历史开局路线表述为当前最佳着或优势来源。
- 2.5 MB压缩运行目录位于`app/data/opening-path-catalog.json`，会随Docker中的`app/`复制进入部署环境，不依赖本地研究目录。
- 阶段报告：`docs/research/chess-analysis-phase8b-opening-lookup-report.md`。

### Phase 8C Kling/Horwitz 残局图盘恢复门禁

- 已下载并拆分1851年 `Chess Studies; or, Endings of Games` EPUB：共185张JPEG，其中绝大多数是单独裁切的残局棋盘。
- 使用MIT许可的本地 `fenshot` ONNX模型进行离线棋盘识别；针对原书斜线底纹和装饰外框增加了多边界候选及置信度仲裁，图片不上传外部服务。
- 185张中183张生成了书本边界候选；严格门禁要求双王完整、棋盘至少对一个行棋方合法、最低格置信度不低于0.70、平均置信度不低于0.90。
- 只有16张通过严格门禁；其余169张全部拒绝进入正式残局库。第一题已正确恢复为黑王d8、黑兵d7、白王d6、白兵d2。
- 已生成16题左右对照人工审核页`docs/research/phase8c-kling-endgame-review-boards.html`；左侧为原书棋盘，右侧为Unicode棋子恢复棋盘，只审核棋子格子、颜色和种类，不审核行棋方、胜负或解答。
- 本轮人工审核通过后，审核过的同版式棋子图可作为模板继续恢复剩余低置信度图；未经审核的数据不会进入`EndgameKnowledgeRepository`。
- 第一批16题审核已导出：13题按页面显示通过，KH-005、KH-012、KH-016被标为错误。裁决确认前两类根因分别为错误旋转180度和候选择优漏子；三题修正后均通过python-chess合法棋盘检查。
- 已保留每张图的20个边界候选，并用第一批审核结果校准本书固定版式的棋盘边界选择。16题逐题留一恢复为16/16，但样本仍小，只作为进入下一人工门禁的依据。
- 183张有候选的图片中，校准后43张具备双王且至少对一个行棋方合法；排除第一批后，12张通过第二批平均置信度、候选分差和合法性门禁。
- 第二批审核页为`docs/research/phase8c-kling-round2-review-boards.html`；完成前不恢复解答、不写入正式残局知识库。
- 第二批12题已全部通过人工棋盘审核；两轮共28个棋盘均已裁决。
- 图注与结果恢复后，20题同时具备明确行棋方、原书结论和七子表一致性，并保留合法关键首着；3题超过七子，仅由Stockfish Depth 20/MultiPV 3支持；4题缺少行棋方或完整目标；KH-007原书称白先胜但Syzygy精确判和。
- 新增只读`EndgameKnowledgeRepository`与`POST /api/endgame-lookup`。运行时只导出20个`exact_verified`局面，必须棋子摆放与行棋方完全一致；不返回相似局面、冲突题、缺失题或仅有普通引擎支持的题。
- 残局查询不调用Stockfish或DeepSeek，尚未接入专业分析Prompt或前端。完整研究数据为`docs/research/phase8c-kling-endgame-dataset.json`，运行时数据为`app/data/endgame-knowledge.json`。

### Phase 8D 多棋书七子残局扩充

- 从现有棋书真值集筛出38个七子以内候选，并逐题使用Lichess Syzygy重新验证胜和负、距离字段和关键首着。
- 33个候选通过准确性与教学内容门禁，5个源范围冲突、书库冲突或无有效教学内容的候选保留在研究审计中但不进入产品查询。
- 与Phase 8C的20题合并后，`app/data/endgame-knowledge.json`现有53个精确残局局面，来源扩展到Kling/Horwitz及另外8本棋书。
- 运行时结构升级为多来源记录；每条可独立保存书名、作者、年份、定位、来源链接、表库关键着和书中着。旧版单来源数据仍兼容。
- 棋书原评仍只绑定完全相同局面；胜和负及程序关键着由Syzygy负责，DeepSeek不参与硬事实生成。
- 《残局教科书》已列为下一优先来源；当前尚未取得实际电子文件，取得后使用同一门禁导入，不从非授权网页抓取正文。
- 报告：`docs/research/chess-analysis-phase8d-endgame-expansion-report.md`；审计：`docs/research/phase8d-endgame-expansion-audit.json`。

### Phase 8E 开局人类解释层

- 使用通过官方MD5校验的Wikimedia Wikibooks快照提取《Chess Opening Theory》；发现3,010个页面，2,948个页面标题可恢复为合法走法路径。
- 清除空页、纯谱表、模板、引用和导航后保留1,896段有实质内容的人类开局解释。
- 现有3,810条Lichess开局目录全部能命中同路径或最深祖先路径解释，覆盖149个家族；801条为完全同路径解释，平均匹配深度6.19个半回合。
- `/api/opening-lookup`新增可选`humanExplanation`，包含原文、匹配深度、来源页面、固定修订号、CC BY-SA/GFDL许可和署名。
- Lichess CC0名称/路径与Wikibooks人类解释保持独立；解释不提供当前评价或最佳着，也尚未进入DeepSeek Prompt或前端。
- 报告：`docs/research/chess-analysis-phase8e-opening-human-explanations-report.md`；数据：`docs/research/phase8e-opening-explanations.json`；运行时：`app/data/opening-explanations.json`。

### Phase 8E Wikibooks 开局解释研究数据

- 工作区现有未提交的Wikibooks `Chess Opening Theory`英文解释数据：60条合法走法路径解释，按最长前缀覆盖3,578/3,810条开局目录记录（93.91%）和135个开局家族。
- 每条解释保留页面标题、URL、修订ID、许可与署名；`OpeningKnowledgeRepository`只按已验证UCI路径精确或最长前缀查找，不让文本参与开局命名。
- 英文原文只描述其来源路径，不是当前Stockfish评价、最佳着、当前威胁或当前计划的事实来源；当前“双方子力与局面”不直接展示或发送这些原文。
- 运行时数据为`app/data/opening-explanations.json`；构建脚本与门禁测试分别为`scripts/build_phase8e_opening_explanations.py`和`tests/test_phase8e_opening_explanations.py`。

### Phase 8F 开局识别接入“双方子力与局面”

- 专业分析会使用当前选中节点及此前全部已验证UCI走法查询`OpeningKnowledgeRepository`；开局身份、ECO和变例完全由程序确定，DeepSeek不参与命名。
- 展示采用保守门禁：至少匹配4个半回合；最长前缀匹配还必须覆盖当前序列至少75%，且最多偏离2个半回合。浅层随机前缀不会触发开局卡片。
- 开局卡片嵌入现有“双方子力与局面”，不新增独立开局模块；未命中、低置信度或缺少受控中文资料时保持原有界面和生成逻辑。
- 当前受控中文资料覆盖意大利开局、西班牙开局、西西里防御、法兰西防御、卡罗-康防御、后翼弃兵、英国式开局和王印度防御；其他数据库名称不会为了扩大显示率而自由生成说明。
- 已确认开局作为`confirmedOpening`传入专业分析Prompt；Prompt禁止DeepSeek重新判断名称、补写变例，或把开局常见思路直接升级为当前局面的事实、计划、评价或威胁。
- 根目录`index.html`与发布版`docs/index.html`保持同步；开局卡片在手机布局下改为单列，避免横向溢出。
- 完整单元测试为`327 passed, 2 warnings`；15局面真实DeepSeek质量套件首次通过与最终严格通过均为15/15、安全回退0/15；另有1个携带“意大利开局 · 吉奥科钢琴变化”的真实请求首次通过且严格校验错误为0。

### 当前未提交的业务相关修改

### 2026-08-11 Phase 8G 中文开局解释与棋书来源扩展

- 现有1,896段Wikibooks开局解释已全部增加`textZh`中文字段，原始英文保留为`textEn`；`OpeningKnowledgeRepository`优先返回中文，旧数据无中文字段时仍兼容回退。
- 中文翻译仅改变展示层，不改变程序确定的开局名称、ECO、UCI/SAN、局面事实、Stockfish评价或当前计划。
- 新增公版来源登记：*Hoyle's Games Modernized*（1898）与 *Foster's Complete Hoyle*（1913），均覆盖开局、中局、残局，作为下一轮章节与图盘抽取来源；已有《蓝皮书》和 Edward Lasker 教材标记为已导入，避免重复收集。
- 已下载上述两本书的Project Gutenberg纯文本研究副本，按章节提取2,693段开局、中局、残局和通用棋理语料；当前仅用于研究检索和人工审核，不作为未经校验的当前局面答案。
- 新增`app/data/book-source-registry.json`与`docs/research/phase8g-book-source-expansion.json`。新来源目前是可追溯研究来源，尚未把未经精确棋盘/走法校验的文字注入生产Prompt。
  - 相关测试：27 passed, 2 warnings；根目录`tests/`完整回归为342 passed, 2 warnings。直接运行仓库根目录pytest仍会因`work/`下两个历史副本存在同名测试而发生pytest模块收集冲突。

### 2026-08-11 Phase 8H 第二轮棋书扩展

- 新增并下载三本公版棋书研究副本：Philidor《Studies of chess》（1803）、Franklin K. Young《Chess Generalship, Vol. I》（1917）和 Richard Penn《Maxims and Hints on Angling, Chess, Shooting, and Other Matters》（1833）。
- 来源覆盖开局原则、中局战略、空间与兵力集中、残局和棋手决策习惯；均登记书名、作者、年份、来源地址和主题分类。
- 修正综合棋书章节边界并移除Gutenberg前言/目录元数据后，研究语料由2本书的2,693段重新整理为5本书的1,373条有效棋类章节记录。新语料仍是研究层，未未经精确棋盘/走法校验直接注入生产Prompt或当前局面答案。
- Philidor旧记谱法与图表例局列入后续逐着恢复任务；七子以内仍要求Syzygy，超过七子只保留Stockfish支持状态。
- Phase 8H自动校验：来源5个、记录1,373条、重复ID 0、未登记来源0、URL格式错误0、过短记录0、元数据警告0。

### 2026-08-11 Phase 8I 新棋书棋局候选扫描

- 对新增棋书原始文本完成候选扫描，共定位157段可能包含棋盘、图表或走法的段落：Studies of chess 37、Hoyle 45、Foster 74、Chess Generalship 1。
- 候选全部标记为`needs_manual_board_recovery`，尚未作为FEN、胜和负或关键着进入运行时库；下一步按旧记谱法和图表可读性逐题恢复。

### 2026-08-11 Phase 8J 全类型棋书统一知识库

- 当前完成收集的棋书材料已统一写入`app/data/unified-book-knowledge.sqlite3`：5,021条精确局面原评、1,373条章节/原则、1,896条开局路径解释，合计8,290条、18个来源。
- 按阶段为开局4,448条、中局2,214条、残局607条、通用棋理1,021条；FTS5全文索引可用，支持阶段和记录类型过滤。
- `UnifiedBookKnowledgeRepository`支持全文搜索和精确FEN查询；数据库随Docker的`app/`目录部署。
- 权威层继续分离：精确局面只绑定完全相同状态；开局解释只绑定已验证路径；章节原则可检索但不覆盖当前局面事实。本阶段没有把章节原文自动注入DeepSeek Prompt。

### 2026-08-12 Phase 8K 开局卡片路径门禁与信息完善

- 专业分析的开局身份改为依据“被分析着之前”的全部已验证UCI着法，和该步走前局面保持一致。
- 开局卡片只在整段历史着法仍是3,810条开局目录中至少一条完整线路的前缀时显示；若后续已经偏离目录，即使还能命中较早的开局祖先，也不再显示，避免过期开局标签。
- 开局信息直接写入“双方子力与局面”正文，不再嵌套独立卡片；依次展示中文开局名称与ECO、所处分支、开局思路与双方常见战略，没有分支名时自动隐藏分支行。
- 首页“意大利开局”验收样例改为目录中已逐着验证的C54主线，并补齐“格列柯进攻/格列柯弃兵”中文分支名，避免旧样例在第9半回合离开目录后按门禁隐藏开局信息。
- 开局展示不再受原8个手写资料家族限制：3,810条目录中的开局只要路径门禁和至少4半回合门禁通过都会生成`openingContext`；当前具有至少4半回合代表路径的133个家族已全部扫描通过。常见主流体系使用中文专名，其余体系以ECO兜底命名，战略说明由首着与中心结构的受控模板生成，不让DeepSeek猜测开局身份。
- 新增43条经典开局深层主线扩展，覆盖35个家族或核心体系，包括西班牙、意大利、西西里（纳吉多夫、龙式、里赫特尔-劳泽尔、舍维宁根）、法兰西、卡罗-康、后翼弃兵接受/拒绝、塔拉什、斯拉夫/半斯拉夫、尼姆佐/后翼/博戈印度、王印度、格林菲尔德、现代别诺尼、本科弃兵、英国式、列蒂、王翼弃兵等。43条均经`python-chess`逐着验证，拒绝0条，深度为6至11.5个完整回合；原3,810条目录中更深线路继续保留。
- 根目录`index.html`与发布目录`docs/index.html`保持一致；新增离库路径和走前节点边界测试。
- 本次开局相关测试为`21 passed, 2 warnings`，两个HTML的内联JavaScript语法通过。按当前HTML运行完整`tests/`为`346 passed, 2 failed`；失败项是当前页面没有“公测体验版”文案且仍直接加载`chess.js`，属于现有HTML与性能测试预期的差异，本次未擅自恢复。此前同一后端变更的15局面真实质量套件首次与最终严格校验均为`15/15`，安全回退`0/15`，缓存响应`502ms`。

### 2026-08-12 Phase 8L 开局全前缀、转置与即时展示

- 开局展示门禁改为“完整历史仍是目录前缀，或当前合法状态与目录任一中间节点完全一致”；不按孤立着法名称猜测开局。3,854条基础与扩展记录形成8,832个唯一走法前缀，均纳入回归测试。
- 位置索引从仅保存线路终点扩展为保存所有中间节点，使不同合法走序到达同一局面时也能按`position_transposition`识别；FEN身份继续包含行棋方、王车易位权和吃过路兵状态。
- `/api/game-review`仍在每一步附加唯一`openingContext`；专业分析改为复用该结果，不再二次识别。前端切换着法时先同步显示整盘分析中的开局名称、ECO、分支和说明，再异步补充专业分析正文，因此开局展示不依赖DeepSeek响应。
- `openingContext`新增解析器版本与数据库修订字段，前端缓存键包含这两个字段；开局目录或解释库变化后旧专业分析缓存不会继续冒充当前开局数据。
- 1,896条中文开局解释开始用于开局思路正文，按已验证路径匹配并在完整句边界压缩；没有可用解释时继续使用受控家族模板。开局身份、ECO与走法仍只由程序目录决定。
- 新增法兰西防御推进变化欧威变化回归：整盘接口在`8...Be7`走前的15个半回合返回C02和“法兰西防御 · 推进变化 · 欧威变化”；同时增加中间转置和全部8,832前缀测试。
- 正式`tests/`回归为`363 passed, 2 failed, 1 warning`；两项失败仍是本阶段之前已记录的“公测体验版”文案和`chess.js`延迟加载预期差异，与本次开局识别无关。直接从仓库根目录运行未限定测试目录的命令还会误收集`work/`下两份旧副本并产生同名模块冲突，因此当前完整测试必须显式指定`tests/`。
- 完整棋谱另返回`openingSummary`，保存本局曾达到的最深已确认开局。逐着查看时优先显示该节点走前开局；第一步、已离开开局库的中后盘节点或专业分析尚未返回时，回退显示整局开局摘要，避免“输入棋谱但当前停在末着所以看不到开局”。
- 开局中文展示增加术语与记谱清理：`White/Black`、白棋/黑棋、白牌/黑牌统一为“白方/黑方”；Wikibooks残留的`/3. e3/`、`/3...c5/`等路径标记规范为`3.e3`、`3...c5`。含明显乱码或中文内容不足的机器译文不再展示，回退使用受控中文开局模板。
- 开局正文排版拆为开局身份、开局说明、白方思路、黑方思路四个层次，说明和双方计划分别成段；手机宽度浏览器验证无横向溢出。
- 开局说明中的每个编号走法分支按`2...d5`、`3.c4`、`3.Bf4`等SAN起点自动拆为独立段落，每段首行缩进`2em`；音译残留“怀特/布莱克”也统一替换为“白方/黑方”。

  已修改：

- `app/api.py`
- `app/analysis_focus.py`
- `app/analysis_report.py`
- `app/models.py`
- `app/opening_knowledge.py`
- `app/position_facts.py`
- `app/professional_analysis.py`
- `app/professional_refs.py`
- `app/professional_validation.py`
- `index.html`
- `docs/index.html`
- `docs/PROJECT_STATUS.md`
- `scripts/build_professional_validation_set.py`
- `scripts/run_professional_quality_suite.py`
- `tests/fixtures/professional_validation_positions.json`
- `tests/test_position_facts.py`
- `tests/test_opening_knowledge.py`
- `tests/test_professional_analysis.py`
- `tests/test_professional_frontend.py`
- `tests/test_professional_quality_set.py`

工作区还有多份未跟踪的历史审查文档、演示文件和辅助脚本。它们不属于当前功能修改，不得在未确认用途前批量删除、提交或覆盖。`docs/PROJECT_STATUS.md` 是后续状态判断的规范入口。

## 2. 项目核心目标与 MVP 范围

“棋盘研究所”是面向 4—12 岁儿童的国际象棋复盘网站。用户导入或粘贴 PGN，系统使用 Stockfish 生成权威棋盘事实与候选路线，并按需使用 DeepSeek 生成中文解释。

当前 MVP 范围：

- 导入、解析和浏览 PGN。
- 显示棋盘、完整回合记录以及当前半回合。
- 使用服务端 Stockfish 分析当前局面和整盘棋。
- 对每步棋给出评价、质量等级、最佳着和参考变化。
- 通过 `python-chess` 校验棋子、格子、SAN/UCI、吃子、将军、将杀、易位和升变事实。
- 生成儿童化走法解释，并在 DeepSeek 不可用时保留 Stockfish 结果。
- 为选定走法生成结构化“专业棋局分析”，包含事实引用、严格校验、纠错重试、缓存和安全回退。
- 按相关性与重要性筛选弱点、王安全、威胁和路线事件，避免把所有底层事实机械展示。
- 提供统一的响应式“棋盘研究所”视觉界面。

当前不属于已实现 MVP 的内容：

- 用户账号、云端棋局库、社交或课程系统。
- 新模型训练。
- 专业分析后的独立“儿童化翻译第二阶段”。该方向讨论过，但尚未实施。

## 3. 当前技术架构

### 前端

- 技术：单页 `HTML/CSS/JavaScript`，使用本地 `chess.js`。
- 开发入口：`index.html`
- GitHub Pages 发布入口：`docs/index.html`
- 运行时后端地址：
  - 本地从 `http://localhost:8080` 打开时使用 `http://localhost:8000`
  - 线上地址由 `runtime-config.js` / `docs/runtime-config.js` 的 `window.CHESS_API_BASE_URL` 提供
- 静态资源：`assets/`；发布副本在 `docs/assets/`
- 两个 HTML 入口必须同步修改并在提交前验证一致。

重要限制：不要直接用 `file:///.../index.html` 验证后端。当前本地识别和 CORS 配置只允许 `http://localhost:8080` 或 `http://127.0.0.1:8080` 等已配置来源。

### 后端

- 框架：FastAPI
- `app/api.py`：API 路由、服务初始化和内存缓存
- `app/config.py`：`.env` 与环境变量读取
- `app/engine.py`：Stockfish 子进程和多路线分析
- `app/game_review.py`：PGN 逐步分析、合法走法事实和路线构建
- `app/quality.py`：走法质量与损失判定
- `app/complexity.py`：简单、普通、复杂局面分类
- `app/position_facts.py`：子力、王安全、兵形和威胁事实
- `app/analysis_focus.py`：相关性、重要性评分和动态栏目选择
- `app/ai_explainer.py`：儿童化 DeepSeek 解释与校验
- `app/professional_analysis.py`：专业分析调用、Token 控制、缓存、重试和安全回退
- `app/professional_refs.py`：事实 ID、棋子 ID、路线 ID 与 ply ID 的引用解析
- `app/professional_validation.py`：专业输出的结构、事实、路线、证据和长度校验
- `app/models.py`：Pydantic 请求和响应模型

当前 API：

- `GET /api/health`
- `POST /api/review`
- `POST /api/game-review`
- `POST /api/move-explanation`
- `POST /api/move-facts`
- `POST /api/professional-analysis`
- `POST /api/analysis-report`

### 分析与数据流

```text
PGN
→ python-chess 解析
→ Stockfish 三条候选路线
→ 棋盘事实包
→ 专业分析重点筛选
→ 紧凑引用式 DeepSeek 请求
→ 严格校验
→ 必要时纠错重试
→ 安全回退或通过后的结构化结果
→ 前端动态栏目
```

### 部署

- 前端：`docs/` 用于 GitHub Pages。
- 后端：根目录 `Dockerfile` 与 `render.yaml` 用于 Render。
- Render 配置为自动部署、`/api/health` 健康检查、Linux Stockfish 路径 `/usr/local/bin/stockfish`。
- 当前配置模型：`deepseek-v4-flash`
- 当前配置 base URL：`https://api.deepseek.com`
- API Key 只允许存在于本地 `.env` 或 Render 环境变量；不得读取、打印或提交真实值。

## 4. 已完成并验证的内容

### 已合并到 `main`

根据最近提交和跟踪文件，以下能力已进入 `main`：

- PR #2：专业棋局分析严格化。
- PR #3：复杂安全回退长度修复。
- PR #4：专业分析引用化、Token 压缩、性能优化、缓存和 15 局面验证集。
- PR #5：专业分析重点筛选与动态栏目。
- PGN 解析、联盟棋谱文件导入、逐步走棋质量、Stockfish 三路线、儿童解释和 DeepSeek 安全集成。
- 专业分析使用 `pieceRef`、`lineRef`、`plyRefs` 和 `evidenceRefs`，后端填入实际棋子、格子、走法和 PV。
- 无意义的“仅未保护”弱点、无现实危险的王安全、固定物质差栏目和普通 PV 吃子已被重点筛选器过滤或归入对应路线。

### 已合并版本的质量数据

来源：`docs/professional-analysis-quality-report.md`、`docs/professional-analysis-focus-report.md` 和 `docs/professional-analysis-quality-results.json`。

- 固定验证局面：15 个。
- 首次严格校验通过：15/15。
- 最终严格校验通过：15/15。
- 安全回退：0/15。
- 输入 Token：4,457—6,726。
- 首次网络响应：11.3—29.1 秒。
- 缓存响应：3ms。
- 修改前弱点 29 项，重点筛选后 2 项。
- 过滤“仅未保护”事实 62 项。
- 过滤无关王安全描述 117 项。
- 移入候选路线内部事件 54 项。
- 过滤普通 PV 吃子 63 项。

这些数字是 2026-07-20 对已合并 PR #4/#5 的验证结果，不代表当前未提交工作区修改已经重新完成相同的真实 DeepSeek 验证。

### 当前工作区修改已验证的部分

当前本地修改实现了：

- 将“小兵研究员说”和专业分析合并为一个默认展开区域。
- 删除旧简略说明、重复的专业分析外层和展开/收起按钮。
- 放大路线标题、战略标签、首着、评价和路线正文。
- 动态隐藏空栏目。
- 前端正文使用自动高度并显式取消省略、行数限制和隐藏溢出。
- 将内部棋子变量名转换为自然中文名称。
- 增加完整句检查；残句优先用已验证事实重建，无法重建时整条删除。
- 长度控制只允许在完整句或完整分句边界压缩，不再从字符中间硬切。

本地验证：

- `136 passed, 1 warning`
- 根目录与 `docs/` 的 HTML 内容一致。
- `git diff --check` 没有空白错误，只有 Windows 换行提示。

测试警告来自 FastAPI/Starlette 对当前 `httpx` 测试客户端组合的弃用提示，不是测试失败。

## 5. 当前正在处理的任务

当前工作停留在“专业分析统一卡片与文字完整性修复”的 P0 本地人工验收完成阶段。

2026-07-24 已完成：

1. 对当前未提交 diff 做最终人工代码审查。
2. 使用仓库固定棋局 `tests/fixtures/game1_selected_position.pgn` 做真实本地 UI 验收，DeepSeek 专业分析正常返回。
3. 在 `1440×1000` 桌面视口和 `390×844` 手机视口检查统一卡片、完整句、内部变量名、文字截断、路线字号、控制台错误和横向溢出。
4. 修复嵌在句子中间的“依据……可判断……”判断过程未被前端清理的问题。
5. 修复候选路线标题行在桌面窄栏中产生 15px 内部溢出的问题。
6. 修复“没有直接威胁”仍可能作为“最大危险”栏目展示的边界情况。

P0 验收结果：

- 页面只出现一个“小兵研究员说”区域，专业分析默认完整显示。
- 旧简略说明和展开/收起按钮未出现。
- 未发现“当前局面为意大。”“黑象(e7)正。”等残句。
- 未发现 `white_b`、`black_pawn`、evidenceRefs、“查看证据”“事实依据”“判断依据”等用户不可见内容。
- 三条 Stockfish 路线正常显示；桌面路线标题 28px、正文 20px，手机路线标题 22px、正文 16px。
- 桌面和手机页面均无横向溢出，专业正文无 CSS 截断，控制台和页面脚本无新增错误。
- 本次只验证了一个固定局面，不能替代 15 局面真实质量套件。

### 2026-07-24 棋盘坐标与 PGN 方向修复

- 已确认旧前端坐标换算在白方视角时上下颠倒，在黑方行棋时又只翻转横轴，导致棋盘格与 PGN 的 `a1`—`h8` 坐标不一致。
- 棋盘现固定为标准白方视角：白方在下，`a1` 在左下，`h8` 在右上；行棋方变化不再自动翻转棋盘。
- 棋子、实战走法高亮和 Stockfish 推荐箭头统一使用同一套标准坐标换算。
- 棋盘左侧显示 `8`—`1`，底部显示 `a`—`h`，开发版 `index.html` 与发布版 `docs/index.html` 已同步。
- 已在 `1440×1000` 桌面视口和 `390×844` 手机视口检查坐标顺序、初始棋子位置、控制台错误与横向溢出；均通过。
- 新增棋盘前端回归测试；完整自动化测试结果为 `136 passed, 1 warning`。

### 2026-07-24 Phase 1：统一事实驱动 AI 链路

- 新增 `app/chess_facts.py`，统一 `ChessFactPackage 1.0`、白方视角评价、实际走法、最佳走法、候选路线、程序事件及各字段来源。
- FEN 只保留在服务端事实包中用于 `python-chess` 校验；三条 DeepSeek 路径的模型可见载荷均不包含 FEN、完整 PGN、Stockfish 日志或未验证路线。
- Stockfish PV 任意一步非法、SAN/UCI 不一致或最终 FEN 不一致时整条路线失败，不再保留合法前缀；`verified=false` 路线不会进入 DeepSeek 载荷。
- `/api/review` 已改为事实包、DeepSeek JSON、ID/棋步/事件校验、一次纠错和程序模板回退，前端请求与响应入口保持不变。
- `/api/move-explanation` 与 `/api/professional-analysis` 已接入同一事实协议；网络失败或未配置 DeepSeek 时使用经过原有验证器检查的安全回退。
- 专业缓存键加入 `factPackageVersion`，避免读取旧协议缓存。
- 完整自动化测试：`137 passed, 1 warning`。
- 15 局面真实 DeepSeek 套件：首次通过 `15/15`，最终通过 `15/15`，重试 `0/15`，安全回退 `0/15`；输入 Token `4,936—7,502`，首次网络响应 `16.0—24.5` 秒，缓存 `4ms`。
- 本阶段未修改威胁分析、战略计划、知识库或 Agent 架构。

### 2026-07-24 Phase 2：程序化动态威胁分析层

- 新增 `app/threat_analysis.py` 与 `ThreatPackage 1.0`。每个威胁都包含程序来源、verified 路线证据、执行方、目标、支持走法、Ignore Test 结果和置信度。
- 第一阶段严格限制为五类：将杀威胁、战术吃子、稳定赢子、升变威胁和中心突破；普通无保护、仅被攻击、普通交换及无评价/路线支持的候选不会升级为威胁。
- `ChessFactPackage 1.0` 只增加默认空的 `threats` 列表，原 position/evaluation/move/route/event 字段与 FEN 隔离规则不变。
- `/api/review` 在事实包构建后、DeepSeek 前运行 `ThreatAnalyzer`，响应兼容地增加 `threats` 数组；未确认威胁时返回空数组。
- Ignore Test 最多选择 3 个非吃子、非将军、非升变、非目标处理的合法安静着；每次请求最多测试 1 个威胁，独立深度 8，总超时 12 秒。受将军时不存在合法“忽略”，不会伪造测试结果。
- DeepSeek 只能用 `threat_id` 返回解释；未知 ID、重复 ID、额外威胁类型、模型生成 SAN/UCI/格子或事实包外事件继续由统一 validator 拒绝。走法解释的旧 `opponentThreat` 展示字段改由后端根据通过校验的 threat explanation 回填。
- 专业分析缓存键加入 `ThreatPackage` 版本，避免旧提示载荷缓存与新协议混用。
- 完整自动化测试：`146 passed, 1 warning`，Phase 1 的 137 项全部保留，新增 9 项威胁层测试。
- 本地 Stockfish 深度 10 抽样：初始安静局面威胁层约 1ms、0 次额外分析；明确赢子局面威胁层约 379ms、1 次批量 Ignore Test。该数据只代表本机抽样，不是线上 SLA。
- 15 局面真实 DeepSeek 套件：首次通过 `15/15`，最终通过 `15/15`，重试 `0/15`，安全回退 `0/15`；输入 Token `4,941—7,507`，首次网络响应 `13.1—20.9` 秒，缓存 `4ms`。报告与原始结果保存在独立验证目录，没有覆盖仓库内既有质量报告。
- 本阶段未实现 Strategic Plan、知识库、RAG、Agent 框架或新的前端威胁展示。

### 2026-07-24 Phase 3：程序化战略计划分析层

- 新增 `app/strategic_plans.py` 与 `StrategicPlanPackage 1.0`。每个计划包含程序来源、执行方、计划类型、目标、支持走法、至少两条 verified 路线、结构证据和置信度。
- 第一阶段严格限制为八类：改善最差棋子、准备中心突破、占领开放线、激活车、改善王安全、攻击可利用弱兵、制造通路兵和有利简化；单条 PV、随机调动、评价方向冲突或结构证据不足时返回空计划。
- 计划识别比较多条 PV 的共同棋子、目标格、兵突破、攻击对象和结构转换，不要求完整走法字符串相同；路线评价差超过 100cp 或方向明显冲突时取消候选。
- `ChessFactPackage 1.0` 兼容地增加默认空的 `plans` 数组；`/api/review` 兼容地返回高置信度计划，未确认计划时返回空数组，前端本阶段没有新增展示改动。
- `/api/review`、走法解释和专业分析都在 DeepSeek 前运行同一个 `StrategicPlanAnalyzer`。DeepSeek 只允许按已有 `plan_id` 返回解释；未知或重复 ID、模型自造计划类型、路线外棋步及无证据事实会被统一 validator 拒绝。
- 专业分析旧 `plans` 生成入口在生产链上被禁用并保留模型兼容字段；最终双方计划由后端按 `plan_id` 合并。DeepSeek 失败时的安全回退也只使用高置信度程序计划，不再把单条 PV 首着包装成战略。
- 战略层完全复用已有 ChessFactPackage、PositionFacts、ThreatPackage 和 verified MultiPV，不新增 Stockfish 调用。合成三路线热身后基准约 `1.37ms/次`；该数据只代表本机抽样，不是线上 SLA。
- 完整自动化测试：`159 passed, 1 warning`，Phase 1/2 的 146 项全部保留，新增 13 项战略层和生产接入测试。
- 15 局面真实 DeepSeek 套件：首次通过 `14/15`，最终通过 `15/15`，重试 `1/15`，安全回退 `0/15`；输入 Token `5,153—7,530`，首次网络响应 `15.2—21.2` 秒。唯一重试来自既有候选路线 `strategyTags` 数量/枚举错误，纠错后通过；全部结果的计划证据和三条路线验证均通过。
- 本阶段未实现知识库、RAG、Agent 框架、自由棋理生成或新的前端计划展示。

### 2026-07-24 Phase 4：专业复盘文本生成层

- 新增 `app/analysis_report.py` 与 `AnalysisReportPackage 1.0`，固定包含局面概况、关键一步、威胁、战略、verified 路线和总结六个章节。评价、优势方向、物质、王安全事实 ID、局面事实 ID、实际/最佳走法、威胁 ID、计划 ID 和路线均由程序写入。
- 新增 `app/narrative_generator.py`。Narrative DeepSeek 只接收去除 FEN、PGN、Stockfish 日志、未验证路线和路线 SAN/UCI 的报告载荷，只能补写章节文本及选择已有总结引用，不能发现事实、计算评价、生成棋步、威胁、战略或路线。
- 新增独立 `POST /api/analysis-report`，请求格式继续使用 `analysis_id` 与 `move_index`。旧 `/api/professional-analysis`、现有前端、儿童化解释、Stockfish、`ThreatAnalyzer` 和 `StrategicPlanAnalyzer` 均未修改。
- 新接口从缓存的 `MoveReview` 直接构建 `ChessFactPackage`、同步检测 `ThreatPackage`、构建 `StrategicPlanPackage` 和 verified routes，不新增 Stockfish 调用，也不调用旧 ProfessionalAnalysis LLM 路径。
- 每个未缓存报告请求最多执行一次 Narrative DeepSeek HTTP 请求；JSON、ID、棋步、评价方向、战略/威胁类型或总结引用校验失败后立即使用程序模板回退，不执行第二次纠错请求。缓存命中不调用 DeepSeek。
- 总结的 `source_refs` 必须至少引用已有 `move_error_id`、`threat_id`、`plan_id` 或 `route_id`；无引用、未知引用、重复引用或正文观点与引用类型不匹配时拒绝模型结果。
- 新增 `tests/test_analysis_report.py` 和新接口回归测试。完整自动化测试结果为 `173 passed, 1 warning`，Phase 1—3 的 159 项全部保留。
- 新增 `scripts/run_analysis_report_quality_suite.py`。15 局面真实 DeepSeek 套件结果：最终有效 `15/15`，单次调用约束 `15/15`，模型输出直接通过 `13/15`，严格校验后程序安全回退 `2/15`。两次回退分别来自评价方向说反、空计划包下生成战略观点；没有放松校验。
- 15 局面程序报告编排耗时中位数 `26.12ms`、最大 `51.29ms`，Narrative 网络耗时中位数 `4.463s`，输入 Token `2,046—2,488`，总 Token `2,393—3,087`。结果保存于 `docs/analysis-report-quality-results.json`，报告保存于 `docs/analysis-report-quality-report.md`。
- 本阶段没有实现儿童化、IP 角色、知识库、RAG、Agent 或模型训练，也没有迁移现有前端。

### 2026-07-26：完整删除关键棋子功能

- 已从 `PositionFacts`、专业分析公开模型、DeepSeek 草稿 Schema、引用目录、Prompt、resolver、validator、fallback、长度裁剪、质量脚本和测试数据中删除关键棋子字段及生成逻辑。
- `/api/professional-analysis` 的 `analysis` 不再返回 `keyPieces`；Prompt 版本升级为 `professional-v10-without-key-pieces`，避免旧内存缓存与新协议混用。
- 根目录与发布目录前端已删除专用读取、转换和栏目渲染；剩余主要顺序为当前局面、最大危险/威胁、实战走法、双方计划和候选路线，不新增替代栏目。
- 完整自动化测试为 `174 passed, 2 warnings`；内联 JavaScript 语法、页面同步、真实 PGN 导入/整盘分析/回放和专业分析接口浏览器流程均通过。
- 15 局面真实 DeepSeek 套件：首次通过 `15/15`，最终通过 `15/15`，重试 `0/15`，安全回退 `0/15`；输入 Token `3,569—6,261`，首次网络响应 `10.142—18.801` 秒，缓存 `6ms`。本次结果写入系统临时目录，没有覆盖仓库内历史质量报告。
- Stockfish、ChessFactPackage、Threat Analysis、Strategic Plan、AnalysisReport、候选路线、局面分析和实战走法分析均保留；未提交、未部署。

### 2026-07-30：Phase 5B 棋书局面解释试验

- 完成三本 Project Gutenberg 棋书的来源、可抽取内容和境外权利边界核验；它们只被确认为美国公版来源，中国及其他产品发行地区的商业再利用权仍需正式复核。
- 抽取 20 个内部研究局面：`Chess Strategy` 8 个 ASCII 棋盘图、`Chess Fundamentals` 6 个旧式描述记谱局面、`The Blue Book of Chess` 6 个 PGN 注释局面。
- 20 个 FEN 与引用走法均通过 `python-chess` 合法性检查；使用 Stockfish 18、深度 16、MultiPV 3 复核评价与候选着。
- 初步裁决为：10 个高度一致、7 个有条件一致、3 个明确分歧。该结果证明棋书适合作为人类解释模式来源，但不能未经引擎和人工裁决直接作为当前局面的事实真值。
- 研究材料保存在 `docs/research/phase5b-source-inventory.md`、`docs/research/phase5b-pilot-findings.md` 和 `docs/research/phase5b-pilot-positions.json`。
- 本阶段没有修改 `app/`、前端、API、Prompt 或测试，没有实现知识库、RAG、Agent，也没有调用 DeepSeek 生成金标准。

### 2026-07-30：Phase 5C 古今棋书候选样本扩展

- 新核对 6 本古今棋书；从 4 本书中恢复 18 个候选局面，其中古典教材 7 个、现代出版社官方样章 11 个。
- 18 个 FEN、行棋方和标注 SAN/UCI 均通过 `python-chess` 合法性检查，覆盖评价来源、战术威胁、王安全、兵结构、子力协调、空间、开放线、关键计划、最差棋子和长期弱点 10 个受控维度。
- Stockfish 18 初筛改为“100,000 节点发现候选，再对每个根着分别使用 250,000 节点复核”，避免直接比较 MultiPV 与单着搜索造成的资源分配误差。
- 初筛结果为 12 个方向一致、4 个需要收窄强度或唯一性措辞、2 个存在引擎张力；全部保持 `reviewStatus: pending`，没有把引擎结果冒充人工裁决。
- Philidor 1803 年古式记谱和旧易位说明、Franklin K. Young 的抽象战略理论暂不自动转成局面样本，避免猜测 FEN 或错误配对理论。
- 研究材料保存在 `docs/research/phase5c-expanded-source-inventory.md`、`docs/research/phase5c-expanded-findings.md` 和 `docs/research/phase5c-candidate-positions.json`。
- 本阶段只新增研究文档和候选数据，没有修改 `app/`、前端、API、Prompt、测试或运行时行为，也没有建设知识库、RAG、Agent 或模型训练流程。

### 2026-07-30：Phase 5C 第一轮人工审核

- 项目负责人完成 18 个棋书候选局面的第一轮人工审核：15 个通过，第 4、5、6 题（`PCT-01`、`PCT-03`、`GRO-01`）要求深入分析。
- 三个待深入局面使用 Stockfish 18、Threads 1、Hash 256 MB，对每个指定根着分别运行 3,000,000 节点，避免不同着法获得不同搜索预算。
- 深入结果：`PCT-01` 的棋理方向成立，但 `...Qe7` 只比最佳防守约差 0.26 兵；`PCT-03` 的 `...Qb8` 约损失 0.61 兵，属于战略不精确但“严重错误”措辞偏重；`GRO-01` 的更深结果支持 `Na3` 略强于实战 `Ra2`，两着均保持明显胜势。
- 项目负责人已经接受三题的深入分析建议：`PCT-01` 和 `PCT-03` 收窄错误强度后通过，`GRO-01` 按“Na3 可能更强”通过；本批 18 个局面全部完成审核。
- 人工审核记录保存在 `docs/research/phase5c-human-review-results.json`，深入分析保存在 `docs/research/phase5c-deep-analysis-456.md`。
- 本轮仍只修改研究资料，没有修改业务代码或运行时行为。

尚未完成：

1. 决定是否创建分支、提交和草稿 PR。
2. 合并后确认 GitHub Pages 与 Render 的实际部署状态。

不要把当前工作区修改描述为已经发布。

## 6. 已确认的产品与 UI 要求

### 产品原则

- 面向 4—12 岁儿童，但不能过度低幼。
- 棋盘是分析页视觉核心。
- DeepSeek 负责解释，不得创造棋子、格子、走法、评价或路线。
- 不得降低严格事实校验来提高通过率。
- 没有值得讲的内容时隐藏栏目，不为了填模板强行生成。
- 证据数据可以保留在后端，但最终用户报告不显示 evidenceRefs、事实依据、判断过程或证据数量。
- 不显示 `white_b`、`black_pawn` 等内部变量名。

### 视觉规范

- 风格：温暖、清爽、圆润的“小小棋盘研究所”。
- 主背景 `#FFF9EF`，卡片 `#FFFFFF`，主文字 `#292725`，次要文字 `#6F6A64`。
- 主按钮 `#F4B942`，辅助浅蓝 `#E8F3F2`，正确 `#55B987`，错误 `#E97878`，边框 `#E8DED1`。
- 使用大圆角、轻阴影和充足留白。
- 不使用暗色背景、霓虹、玻璃拟态或复杂动画。
- 桌面、平板和手机都不能横向溢出。

### 专业分析展示

- 只保留一个“小兵研究员说”区域，专业分析默认展示。
- 栏目动态出现，可能包含：当前局面、最大危险、双方计划、弱点、王安全、实战走法和三条 Stockfish 路线。
- 路线内部事件只能显示在对应路线中。
- 文本必须完整、自然、具体；不能显示“当前局面为意大。”“黑象(e7)正。”等残句。
- 无可靠 ECO/开局库来源时，不生成具体开局名称。
- 正文不得使用 `line-clamp`、省略号、固定高度或隐藏溢出来掩盖内容。

## 7. 已发现但尚未解决的问题

### 已确认

- 本地若从 `file:///` 打开页面，后端连接会失败。必须分别启动 8000/8080 服务并访问 `http://localhost:8080`。
- 本地后端和静态服务器不是持久服务，终端或运行会话结束后需要重新启动。
- 当前工作区有未提交和未跟踪文件，提交前必须精确选择文件，不能执行批量清理。
- `docs/professional-analysis-focus-report.md` 的 Git 修改标记没有可见文本 diff，原因待确认。
- Phase 3 的 15 局面真实 DeepSeek 原始结果和分批报告保存在独立验证目录，未覆盖仓库内既有质量报告。

### 待确认

- 当前 Render 生产服务是否仍在运行、是否会休眠、最新部署提交是什么。
- 当前 GitHub Pages 是否已经指向 `022a5bf`，以及是否包含本地未提交 UI 修改；按 Git 状态判断，本地修改尚未发布。
- `deepseek-v4-flash` 在当前生产账户下的实际可用性、延迟和费用。
- 截图对应同一盘棋、同一回合的棋理内容是否符合专业棋手预期；P0 已完成页面结构、完整性和响应式验收，但没有替代专业棋理人工评审。

## 8. 测试、性能与已知限制

### 每次修改后的基础测试

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

当前结果：173 项全部通过。

### 专业质量验证

- 固定局面：`tests/fixtures/professional_validation_positions.json`
- 自动测试：`tests/test_professional_quality_set.py`
- 质量脚本：`scripts/run_professional_quality_suite.py`
- 报告：`docs/professional-analysis-quality-report.md`
- 原始结果：`docs/professional-analysis-quality-results.json`

真实质量套件会调用 Stockfish 和 DeepSeek，不能用普通单元测试结果替代。

### 已知限制

- Render 免费实例可能冷启动，首次请求延迟不稳定。
- 质量报告中最慢网络响应为 29.1 秒，超过原定复杂局面 25 秒目标；报告说明该次包含网络传输重试。
- 专业分析缓存是服务端内存缓存，服务重启后失效。
- 游戏分析缓存同样为内存状态，前端持有的 `analysis_id` 在后端重启后不可继续使用。
- MVP 没有数据库、用户账户或持久化分析记录。

## 9. 仅讨论过但尚未实施的想法

- 在专业分析之后增加独立儿童化翻译流程。
- 用户账户、棋局历史、课程与训练计划。
- 将本地开发服务做成开机自动运行或桌面启动器。

这些内容不得在没有新需求和范围确认时自动开发。

## 10. 下一步建议

### P0：保护并验收当前工作区

已于 2026-07-24 完成。验收范围与结果记录在第 5 节。

### P1：重新验证质量

1. 运行 15 局面真实 DeepSeek 套件。
2. 确认首次通过率、最终通过率、回退率、Token 和耗时没有退化。
3. 对至少三个代表局面人工检查最终中文内容。

### P2：整理提交

1. 新建 `codex/` 前缀分支。
2. 只暂存本次相关文件；排除 `.env`、演示文件、旧辅助脚本和用途不明的未跟踪文件。
3. 创建草稿 PR，等待确认后再合并。

### P3：部署验收

1. 合并后检查 Render 与 GitHub Pages。
2. 检查 `/api/health`、Stockfish、DeepSeek 配置和 `/api/professional-analysis`。
3. 用固定 PGN 做生产冒烟测试，且不得记录密钥。

## 11. 压缩聊天记录后的继续指令

建议直接发送：

> 请先完整阅读根目录 AGENTS.md 和 docs/PROJECT_STATUS.md，然后检查当前 git status 与未提交 diff。继续“专业分析统一卡片与完整句修复”的 P1 质量验证：运行 15 局面真实 DeepSeek 套件，检查首次通过率、最终通过率、回退率、Token、耗时和至少三个代表局面的最终中文内容；不要提交、合并或部署。

## 12. 2026-07-31 Phase 5D 棋书局面基线评测

Phase 5D 已完成评测、诊断和架构建议，未修改业务代码。

评测输入：

- `docs/research/phase5c-candidate-positions.json`
- `docs/research/phase5c-human-review-results.json`
- `docs/research/phase5c-deep-analysis-456.md`
- 18 个局面均已由项目负责人完成最终审核。

真实运行：

- Stockfish 18，Depth 10，Threads 1，Hash 32 MB，MultiPV 3。
- 当前配置模型 `deepseek-v4-flash`。
- 18/18 局面完成 PositionFacts、ThreatAnalyzer、StrategicPlanAnalyzer、Professional Analysis、Analysis Report 和 DeepSeek 两条文本链。
- 运行错误 0。
- Professional Analysis 首次校验失败后重试 2 题，最终安全回退 0 题。
- Analysis Report 安全回退 4 题。
- 棋书原文和人工答案没有进入模型 Prompt。

质量结论：

- 严格棋理通过 0/18。
- 核心棋理较完整复现 3/18，部分触及 10/18，遗漏或方向相反 5/18。
- 战略主题遗漏 15 题，最终文本棋盘/走法事实错误 13 题，战术判断错误 7 题，错误计划 7 题，无证据主动权判断 2 题。
- 当前真正缺少的是位于事实/路线与 DeepSeek 之间的程序化“局面解释层”，而不是知识库、RAG、训练或 Agent。

Phase 5D 文件：

- 正式规则：`docs/research/phase5d-evaluation-rubric.json`
- 原始真实输出：`docs/research/phase5d-baseline-raw-results.json`
- 紧凑证据：`docs/research/phase5d-compact-evidence.json`
- 逐题裁决：`docs/research/phase5d-adjudication.json`
- 最终报告：`docs/research/chess-analysis-phase5d-baseline-report.md`
- 可重复运行脚本：`scripts/run_phase5d_baseline.py`
- 证据压缩脚本：`scripts/summarize_phase5d_results.py`

Phase 6 建议优先级：

1. 隔离当前威胁与 PV 内部事件。
2. 新增最小 `Position Interpretation Package`，输出评价来源、主题优先级、计划因果、措辞边界和禁止推断。
3. 物质、王位置、易位、是否首选等字段改用程序受控措辞。
4. 为“主动权”增加动态证据门禁。
5. 再补弱格、空间、预防、交换目的、最差棋子目标和计划时机检测。

本阶段只提出 `app/position_interpretation.py` 的建议输入输出结构，没有创建或修改该业务模块。

## 13. 2026-07-31 Phase 6A 威胁作用域与硬事实保护

Phase 6A 已完成最小业务代码修改，未提交、推送或部署。

已实现：

- `current_direct_threat`、`prepared_threat`、`route_event`三类威胁作用域。
- PV后续普通吃子、将军、交换和升变不再自动成为当前全局威胁。
- 准备型威胁必须有明确准备着、执行着在忽略应对后仍合法，并通过Ignore Test。
- 物质差、王位置、易位边界、评价方向、走法质量及首选一致性改由程序模板控制。
- Stockfish分数不再直接推出主动权；主动权需要当前动态威胁和至少两条强制应对路线。
- 事实不足时initiative为`unknown`，DeepSeek不得输出主动权结论。

验证结果：

- 完整pytest：`186 passed, 2 warnings`。
- 18题真实套件：18/18完成，运行错误0。
- 严格通过：2/18；Phase 5D基线为0/18。
- 棋盘事实错误：13→0。
- 战术判断错误：7→2。
- 无证据主动权：2→0。
- 战略主题遗漏：15→15。
- 错误计划：7→7。
- Analysis Report安全回退：4→15。
- CC-035、CC-036、GEL-03的PV内部吃子均只保留为`route_event`。

Phase 6A文件：

- 最终报告：`docs/research/chess-analysis-phase6a-validation-report.md`
- 原始真实输出：`docs/research/phase6a-validation-raw-results.json`
- 紧凑证据：`docs/research/phase6a-compact-evidence.json`
- 逐题裁决：`docs/research/phase6a-adjudication.json`

下一阶段可以进入Phase 6B，但范围应限定为战略主题识别和计划门禁。不要增加知识库、RAG、训练或Agent；不要继续用Prompt承担程序尚未识别的棋理。

## 14. 2026-08-02 Phase 6D 人类局面样本候选与可视审核

- 已整理 32 个不与 Phase 5C 既有 18 题重复的候选局面，保存在 `docs/research/phase6d-human-case-library-candidates.json`。
- 32 个 FEN 均通过合法性检查；全部保持 `pending_human_review`，尚未作为事实真值、Prompt 示例或训练数据使用。
- 已生成 `docs/research/phase6d-human-case-review-boards.html`，使用项目现有透明棋子图片直接显示棋盘，并支持着法高亮、来源筛选、通过/修改/删除、审核备注和 JSON 导出。
- 页面生成脚本为 `scripts/build_phase6d_board_review_page.py`；本步骤没有修改业务分析链、Prompt、模型或 API。
- 第一轮人工审核已导出并归档到 `docs/research/phase6d-human-review-results.json`：20 题通过、9 题需要修改、2 题删除、1 题未审核（`BB-05`）。
- 审核处理清单见 `docs/research/phase6d-human-review-summary.md`。需要修改、删除和未审核的局面尚未进入正式样本库。
- 第二轮处理已完成：20题归入 `phase6d-approved-human-cases.json`，2题保持删除，10题进入 `phase6d-round2-review-candidates.json`。
- 对CS-06、CF-03、CF-06、BB-01、BB-05、BB-06、CC-037使用Stockfish 18执行同预算深搜：候选发现250,000节点，每个候选根着单独3,000,000节点。
- 关键收窄：CF-03的Nd7仅约损失0.46兵；CF-06的Rb1仅约损失0.14兵；CC-037的h6仅约损失0.19兵；BB-06的Qe7约损失1.69兵；BB-05的Bb3比次选约好1.60兵。
- 第二轮可视页面为 `docs/research/phase6d-round2-review-boards.html`，只含10个待复核局面；完整说明见 `phase6d-round2-analysis-report.md`。

## 15. 2026-08-04 Phase 7B 多棋书人工评注检索库

- 项目负责人明确将第二步调整为多棋书人工评注检索库；该决定替代此前“本阶段不建知识库/RAG”的研究边界，但仍不允许棋书文字覆盖当前局面的程序事实和Stockfish证据。
- 已登记并下载8本Project Gutenberg美国公版棋书；原始全文和图像只保存在被忽略的`work/research_books/`，不进入Git或生产包。
- 已建立离线SQLite语料库`work/research_books/phase7b-book-corpus.sqlite3`：326条人工评注局面、323个不同棋盘；BB 228、CC 42、CF 56，FTS5全文索引可用。
- 新增《Chess Fundamentals》自动抽取：61个FEN辅助索引条目中57个通过python-chess合法性检查，最终56个同时具有稳定原文范围并进入数据库；FEN逐图盲对照仍待完成。
- 已下载《Chess Strategy》167张原始棋盘并测试MIT许可的书籍棋盘识别器；仅1张达到严格可靠阈值、11张满足双方各一王，低置信结果全部排除，没有为了扩量放松合法性门禁。
- 新增离线`BookCorpusRetriever`，按子力、兵线、王区、阶段、行棋方、棋子粗区域和程序主题返回最多5条原评；Prompt载荷带来源、长度上限和“不得复制棋子、走法、评价”的硬边界。
- 检索接口尚未接入DeepSeek生产链。下一步必须先完成50–100条可验证棋理规则，再为326条语料补Stockfish/规则/威胁作用域标签并做排除同盘、排除同书的盲检索评测。
- 完整pytest：`218 passed, 2 warnings`；新增5个抽取、数据库和检索测试全部通过。
- 完整报告：`docs/research/chess-analysis-phase7b-book-corpus-report.md`；语料清单：`docs/research/phase7b-book-corpus-manifest.json`。

## 16. 2026-08-05 Phase 7J 棋理重要性排序层

- 已为 Phase 7I 的 5,021 条棋书原评建立离线主题标签；1,700 条能提取明确主题，3,321 条短评、纯变例或无明确主题的原评保持 `unknown`，不进入主题准确率分母。
- 数据按整本书划分：校准集 2,703 条、盲测集 2,318 条；同一本书不会同时进入两边。
- 已对 4,848 个唯一局面运行 Stockfish 18 Depth 8 MultiPV 3、事实、威胁、棋理规则、战略计划、因果排序和当前 PositionInterpretation；另完成 4,843 个不同书中着法后局面的评价，运行错误 0。
- 新增实验版 `PositionImportanceRanker`，只重排可靠程序信号，输出第一主题、辅助主题、证据、可信度和禁止结论；棋书原文、相似局面文本和 DeepSeek 输出都不是排序器输入。
- 整书盲测修改前 PositionFactorRanker 为 Top-1 35.45%、Top-3 76.37%；实验排序器校准后为 Top-1 44.63%、Top-3 76.86%。
- 错误计划、评价强度越界、无证据主动权和 PV 路线事件升级均为 0；事实、路线和主动权门禁没有放松。
- 盲测未达到 Top-1 70%、Top-3 90% 的生产门槛，因此排序器保持离线，未接入 PositionInterpretation、API 或 DeepSeek Prompt。
- 完整 pytest：`276 passed, 2 warnings`。本阶段未训练 DeepSeek、未使用 Agent、未提交、未推送、未部署。
- 报告：`docs/research/chess-analysis-phase7j-importance-ranking-report.md`；结果：`docs/research/phase7j-importance-ranking-results.json`；摘要：`docs/research/phase7j-importance-ranking-summary.json`。

## 17. 2026-08-06 Phase 7K 结构化棋书视角选择先导实验

- 新增两段式棋书视角选择：独立DeepSeek调用可以读取最多5条候选原评，但只能输出案例ID、五类主题、固定原因码和可信度；最终指导包不含原文、来源FEN、棋步、评价、胜负结论或案例ID。
- 精确局面库仍保留全部5,021条原评；相似视角选择只使用1,700条具有明确可追溯主题的案例，3,321条主题不明确原评不强行参与相似迁移。
- 对Phase 7J旧盲测书籍进行五主题各10题、共50题真实DeepSeek诊断；候选只来自校准书籍，同书泄漏0，精确同盘候选0。由于旧盲测结果已公开，本轮仅为诊断，不是新的生产门禁。
- 当前PositionImportanceRanker为Top-1 26%、Top-3 76%；检索第一案例为20%/34%；DeepSeek选择器为24%/56%；程序第一主题加DeepSeek辅助主题为26%/78%。
- DeepSeek修正4个程序第一主题错误，同时破坏5个原本正确结果，净效果为负；2题纠错重试后仍违反“主题必须由所选案例支持”，均被安全拒绝。
- 选择输出保持枚举化；来源原文、棋步和评价进入最终指导包均为0。没有接入PositionInterpretation、API或正式DeepSeek Prompt。
- 完整pytest：`282 passed, 2 warnings`。未提交、未推送、未部署。
- 报告：`docs/research/chess-analysis-phase7k-structured-book-perspective-pilot-report.md`；结果：`docs/research/phase7k-perspective-selector-diagnostic-results.json`。

## 18. 2026-08-06 Phase 7L 主题级因果重要性

- 新增`ThemeCausalImportanceAnalyzer`，按主题聚合全部MultiPV覆盖、稳定路线覆盖、替代路线评价损失、静态主题根着后持续性和当前直接威胁证据；输出`confirmed/supporting/unproven`、因果分数和禁止结论。
- 修正两组主题语义映射错误：`evaluation.tactical/king_safety/structure/activity_source`不再统一归入转换主题；最差棋子、开放线、中心突破、王安全等`plan.*`规则按真实主题归类。主动权门禁和跨主题预防规则不参与主题排序。
- 复用Phase 7J的4,848个真实Stockfish证据包运行5,021条记录，错误0；不调用DeepSeek，不读取棋书原文作为排序输入。
- 旧盲测诊断中，Phase 7J排序为Top-1 44.63%、Top-3 76.86%；修正映射的未校准排序为35.84%/77.73%；主题因果分数直接排序为27.54%/70.80%。
- 修正映射后王安全、兵结构、子力活动的第一主题命中分别提高到15.95%、45.21%、51.53%，但强制战术从81.29%降到44.52%；说明主题更均衡但仍不会稳定选择作者第一重点。
- 因果证据共799个confirmed、14,902个supporting、2,105个unproven。该层适合未来控制结论强度，不适合直接替换第一主题。
- 旧盲测已经公开，本轮不具备新生产门禁资格；未接入PositionInterpretation、API或正式Prompt。
- 完整pytest：`287 passed, 2 warnings`。未提交、未推送、未部署。
- 报告：`docs/research/chess-analysis-phase7l-theme-causal-importance-report.md`；结果：`docs/research/phase7l-theme-causal-benchmark-results.json`；摘要：`docs/research/phase7l-theme-causal-benchmark-summary.json`。

## 19. 2026-08-06 MVP专业分析稳定化

- 当前工作从继续扩展研究模块切换到最小可用产品收敛；Phase 7J、7K、7L实验模块仍未接入生产API或DeepSeek Prompt。
- 专业分析新增程序专属结论整句保护：DeepSeek在自由文本中重写物质、王位置、易位、评价方向、走法质量、首选一致性或主动权时，违规整句会被删除或用中性完整句重建；严格校验规则没有放宽。
- 专业分析缓存版本已更新，避免旧安全回退继续命中；真实质量套件可以从项目根目录直接运行。
- 15局面真实Stockfish + DeepSeek套件由首次通过5/15、安全回退8/15，改善为首次通过15/15、重试0、安全回退0；最终严格通过仍为15/15。
- 本轮记录29处整句边界保护；三条路线、最大危险证据、双方计划证据均为15/15有效，最终校验错误0。
- 完整pytest：`288 passed, 2 warnings`；`/api/health`返回200，Stockfish可用、DeepSeek配置有效；开发版与发布版前端规范化内容一致。
- 当前可以进入5—10盘页面体验验收。提交、推送和部署仍需项目负责人另行授权。
- 报告：`docs/research/chess-analysis-mvp-readiness-report.md`；真实结果：`docs/professional-analysis-quality-results.json`、`docs/professional-analysis-quality-report.md`。
- 按产品负责人最终页面审核意见，开发版和发布版继续保留“小兵研究员说”专业分析面板及`/api/professional-analysis`调用，并删除面板内“当前局面分析”摘要卡片；其余展示范围以后续“结论模式”规则为准。
- 后续页面审核将专业分析前端进一步收敛为“结论模式”：只显示明确威胁、双方计划、王安全、值得关注的弱点，以及精确命中的棋书原评；不显示最大危险推导、实战走法分析过程、候选路线展开、PV、评价变化说明、过程性后果和原始JSON。无有效结论时整个专业面板自动隐藏；后端完整分析结果与严格校验不变。

## 20. 2026-08-10 7天公测版

- 当前产品本来没有登录或注册门槛；页面现已明确标注“公测体验版 · 免费使用”，并同时支持粘贴PGN和选择本地`.pgn`文件，完整Stockfish与DeepSeek流程不降级。
- 新增匿名Analytics：浏览器用`localStorage`保存随机`visitor_id`；后端SQLite记录访客首次/最后访问、设备摘要、来源，以及`page_view`、`upload_pgn`、`analysis_start`、`analysis_complete`事件。没有新增个人信息字段，也不持久化IP。
- 新增`analysis_logs`，记录PGN长度与哈希、走子数、Stockfish/DeepSeek/总耗时、Prompt/Completion/总Token和成功/失败状态；Analytics写入失败不会中断核心棋局分析。
- 新增`GET /api/admin/statistics`，生产环境通过`X-Admin-Key`访问；返回当日访客、上传、分析、成功率、平均耗时、Token和可选成本估算。成本单价通过环境变量配置，默认不猜测价格。
- 新增仅针对异常流量的内存保护和DeepSeek全局突发保护；前端不展示IP、Token或成本限制。棋谱上限统一为100个完整回合（200个半回合），Stockfish深度、MultiPV、分析局面与DeepSeek Prompt未降低或修改。
- 完整pytest：`336 passed, 2 warnings`；两个前端内联JavaScript语法通过；开发版与发布版SHA-256一致；实际启动FastAPI后`/api/health`、`/api/event`和`/api/admin/statistics`分别返回200、202、200；Edge无头浏览器的桌面、平板、手机视口均无横向溢出、控制台错误或资源加载错误。
- 15局面真实Stockfish + DeepSeek质量套件已重跑：首次校验通过15/15、最终严格通过15/15、安全回退0/15、三条路线15/15有效，缓存响应312ms；正式结果和报告已刷新。
- 当前SQLite路径可通过`ANALYTICS_DB_PATH`配置。Render生产环境如需跨重启保留完整7天数据，部署前仍需确认该路径位于持久卷；当前仓库没有擅自升级付费实例或挂载付费磁盘。
- 公测能力现已在首页显式展示为“无需注册、完整分析、免费公测”三项，并提供匿名数据说明；通过`file:///`打开时会显示文件预览警告并停止后端健康检查，提示改用`http://localhost:8080`。HTTP桌面、平板、手机视口均显示新说明且无横向溢出或控制台错误。

## 21. 2026-08-11 独立商业化运营后台

- 新增独立`admin.html`与发布副本`docs/admin.html`；产品首页没有后台入口，也不展示Token、成本、异常保护或生产配置。管理员密钥只保存在当前标签页的`sessionStorage`，不会写入页面或仓库。
- 新增`GET /api/admin/dashboard`，支持按日期读取访问人数、页面访问、上传成功/失败、分析次数、转化率、成功率、Stockfish/DeepSeek耗时、Prompt/Completion/总Token、估算成本、最近50条匿名分析日志、保护规则和生产配置状态。
- 原`GET /api/admin/statistics`保持兼容并增加日期参数；生产环境的两个管理员接口均继续通过`X-Admin-Key`保护。
- 为降低7天公测固定成本，Analytics存储已支持本地SQLite与生产Postgres双后端；Render Blueprint保持免费Web实例并新增免费`pawnlab-analytics` Postgres，通过`ANALYTICS_DATABASE_URL`连接。管理员密钥自动生成，DeepSeek V4 Flash成本估算单价按2026-08-11官方价格配置为输入`$0.14/百万Token`、输出`$0.28/百万Token`。
- 后台专项测试`23 passed`；完整pytest`341 passed, 2 warnings`。Edge真实浏览器桌面与手机均显示10个指标卡和4组保护规则，无横向溢出、控制台错误或产品页链接。两个后台页面内联JavaScript语法通过，Render Free Web + Free Postgres YAML解析通过。
- 15局面真实Stockfish + DeepSeek质量套件：首次通过15/15、最终严格通过15/15、安全回退0/15，缓存响应410ms；说明后台与统计改动没有降低正式分析质量。

## 22. 2026-08-11 前端视觉体验升级

- 产品首页在不修改页面结构、用户可见文字、分析逻辑、数据结构、API调用和棋盘交互的前提下完成纯CSS视觉升级；开发版`index.html`与发布版`docs/index.html`保持完全一致。
- 统一强化暖米白背景、白色大圆角卡片、黄色主按钮、浅蓝浅绿辅助卡、柔和分层阴影与留白；棋盘增加白色外框、暖色与薄荷色装饰光晕，但棋子、坐标、高亮、箭头和棋步交互层保持原样。
- 新增页面模块淡入上移、卡片错峰进入、按钮悬停浮起和点击缩放反馈，并继续遵守`prefers-reduced-motion`减弱动画设置。
- 完整主测试目录回归`341 passed, 2 warnings`；开发版与发布版内联JavaScript语法通过。Edge真实浏览器桌面、平板、手机视口导入示例PGN后均恢复12着，无横向溢出、控制台错误或首屏提前加载`chess.js`。
- 本轮未运行15局面真实DeepSeek质量套件：改动仅限CSS展示层，未涉及提示词、Token、性能策略或专业内容质量。

## 23. 2026-08-11 Render 后端实例升级

- 针对后端分析首次请求加载慢的问题，将 `render.yaml` 中 API Web Service 从 `free` 升级为最低付费实例 `starter`，保持 DeepSeek 模型、分析参数、免费 Postgres 与 API 行为不变。
- Render 官方说明 Free Web 服务空闲 15 分钟后会休眠，唤醒约需 1 分钟；付费实例不会因空闲休眠。该改动仍需在 Render 控制台确认账单并重新部署后才会生效。
- 本轮待部署后复测 `/api/health`、首个分析请求耗时与后台 DeepSeek/Stockfish 分项耗时。

## 24. 2026-08-13 开局说明语义分段修正

- 开局棋书说明不再按每个走法编号机械换段；后端保留原书自然段，前端只按这些自然段显示。属于同一分支的连续走法（例如 `5.Nc3 Bb4 6.Nxc6 bxc6 7.Bd3`）保持在同一段，独立分支才另起一段。
- 说明截取上限提高并改为按完整自然段收录；极长首段只允许在中文完整句末截断，不再把 SAN 中的句点误认成句末，也不再留下 `4..` 一类残片。
- “White / Black / 怀特 / 布莱克”等颜色词继续统一为“白方 / 黑方”；开发版与发布版页面保持一致，每段首行缩进两格。
- 开局知识与前端专项测试 `41 passed`；开发版、发布版内联 JavaScript 语法通过；相关真实施密特变体样例输出为 5 个原书自然段，段内连续走法未被拆散。

## 25. 2026-08-13 开局棋子术语统一

- 开局棋书说明统一采用中文国际象棋常用棋子名称：“主教”改为“象”，“骑士”改为“马”；除这些指定术语外不扩展改写其他用词。
- 对“城堡”进行语境区分：表示 rook 棋子时改为“车”，表示 castling 动作时改为“易位”，并规范“王翼易位、后翼易位、长易位、短易位、易位权”等表达。
- 后端正式输出与前端旧缓存兜底同步处理，开局显示缓存版本升至 7；扫描全部 1,896 条开局说明后，可正常规范化的 1,895 条中“主教、骑士、城堡”残留均为 0。
- 抽查西班牙开局、意大利开局、西西里防御、后翼弃兵、王印度防御和伦敦体系，棋子术语均符合规则。

## 26. 2026-08-13 经典开局中文主动审核

- 主动生成并逐项审核 22 类经典开局的最终页面说明，范围包括西班牙、意大利、苏格兰、维也纳、王翼弃兵、法兰西、卡罗-康、西西里、斯堪的纳维亚、皮尔茨、阿廖欣、后翼弃兵、斯拉夫、尼姆佐印度、后印度、王印度、格林菲尔德、加泰罗尼亚、英国式、荷兰、伦敦和列蒂。
- 新增中文语言质量门槛：出现明显机翻错词、孤立数字残段或损坏走法标记时，不展示该棋书段落，改用项目内已校验的自然中文战略摘要；正常的详细棋书说明继续保留。
- 已拦截的典型问题包括“意大利足球、电子兵、黑人玩家、英语开场、柏林号、播放e5、马节奏、该脚、双足象、用剑、开放理论”等；后端正式输出与前端旧缓存兜底使用同一审核原则。
- 开局显示缓存版本升至 8，避免浏览器继续显示审核前的旧说明。

## 27. 2026-08-13 棋书评价方式接入

- 对 5,021 条公版棋书原评做评价动作统计：41.6%包含明确判断，23.8%说明后果，19.0%进行比较，12.4%解释原因，12.2%讨论对手资源；据此把“先判断、再解释机制与后果、按需比较路线和对手资源、最后落到计划”固化为结构化评价框架。
- 新增 `BookEvaluationStyle`。该框架只学习棋书的评价顺序与语气强度，不迁移来源局面的棋子、格子、着法、评价、胜负、计划或主题判断；判断强度继续受程序评价和证据可信度约束。
- 专业分析 Prompt 版本升至 `professional-v14-book-evaluation-method`，新增 `bookEvaluationMethod`；强制局面优先覆盖对手资源，战略与残局局面优先落到已验证计划，低可信度局面使用谨慎语气。
- 专项回归 `76 passed`；15 局真实 Stockfish + DeepSeek 质量套件最终严格通过 `15/15`、首次通过 `14/15`、安全回退 `1/15`，三条路线、危险引用和计划引用均为 `15/15`。结果保存在忽略目录 `work/book-evaluation-style/`，未覆盖历史质量报告。

## 28. 2026-08-15 单局面分析前端移除

- 移除产品页面中 P1 的整块单局面分析前端：局面评估、更好的走法、简单提示和反馈区域，以及对应的自动评价开关和 `/api/review` 浏览器调用。
- 保留 P2 的逐步走法复盘卡片、小兵研究员说明、Stockfish 走前/走后评价和“重新评价整盘棋”；整盘评价继续通过 `/api/game-review` 调用。
- 后端 `/api/review` 接口和 Stockfish 单局面能力暂不删除，供后续后端兼容或其他调用使用；开发版 `index.html` 与发布版 `docs/index.html` 已同步。
- 线上域名 `pawnlab.cn` 已切换到当前 GitHub Pages 发布内容；`api.pawnlab.cn` 后端 DNS 未修改。DNS 仍需等待部分 IPv4 递归缓存自然过期，IPv6 直连已验证新页面。

## 29. 2026-08-17 Analytics 历史累计统计

- 保留现有按日统计的字段和日期筛选，在 `/api/admin/statistics` 与 `/api/admin/dashboard` 的 `statistics` 中新增 `all_time`，汇总当前 Analytics 数据库保留的全部访客、事件、分析、耗时、Token 和成本记录。
- 原始 `events`、`analysis_logs` 不按天删除；新增跨日期回归测试确认查询指定日期时，当日数据与历史累计数据同时正确返回。
- Analytics/API专项测试 `20 passed`；完整 pytest `289 passed, 3 failed`，失败项均为当前工作区既有前端改动导致的页面断言差异，与本轮后端统计改动无关。

## 30. 2026-08-18 后台历史统计展示

- 后台页面新增“所选日期统计”和“历史累计统计”两组指标；历史组读取 `statistics.all_time`，覆盖访客、页面访问、上传、分析成功率、耗时、Token、转化率和估算成本。
- 保留日期筛选、当天漏斗和当天最近分析日志；历史累计按 Analytics 数据库当前保留的全部记录计算，不删除或重置原始数据。
- 根目录 `admin.html` 与 `docs/admin.html` 已同步，相关 API/Analytics 测试 `20 passed`；后台前端结构断言已补充。

## 31. 2026-08-18 首页反馈系统

- 首页底部新增与现有视觉一致的反馈区，支持 1—5 分评分、最多 2,000 字建议和提交状态提示；评分按钮支持键盘聚焦，建议框在手机端自动切为单列布局。
- 反馈通过现有匿名 Analytics 事件入口提交，只保存匿名访客标识、评分、建议和页面路径，不收集姓名、邮箱等个人信息；后端 `feedback` 事件增加输入校验并兼容已有 SQLite/Postgres 表结构。
- 根目录 `index.html` 与 `docs/index.html` 保持一致；内联 JavaScript 语法检查通过。完整 pytest 为 `289 passed, 3 failed`，失败为本轮之前已存在的前端断言差异，未发现由反馈改动引入的新失败。

## 32. 2026-08-31 专业分析人类教练表达复审

- 确认“像AI”的主要来源不是知识库未连接，而是严格事实保护删除越界句后使用报告式兜底，以及前端先展示栏目和路线、没有先给用户决策结论。
- 专业分析提示词升至 `professional-v27-concise-coach-first`：要求先讲这步解决什么、对手最强回应和直接后果，并禁止“程序记录、依据Stockfish、改善子力协调、逐步施压、导致局面恶化”等报告腔或空泛棋评。
- 事实保护保持原强度；被删除的危险后果现在会重建为完整自然句，战略标签超限时只保留前三个合法标签，空白优点和风险不再进入结果。
- 首页专业分析新增“先说结论”，按走法评价、关键问题、实战意图和首选原因排列；详细局面与三条研究路线继续保留在后面。根目录与发布目录页面内容一致。
- 人工复审页 `work/human-review-v25-round3/boards.html` 已改为第四轮简练教练表达，共7个局面，可逐项标记通过或需修改。
- 相关专项测试 `99 passed, 2 warnings`（新增标签归一化测试后需以最终复跑为准）；内联JavaScript语法检查通过。完整 pytest `341 passed, 3 failed`，3项均为此前已记录的前端断言差异。
- v27真实15局面运行最终严格有效15/15；初次整套运行有1个安全回退，修正漏处理的事件说明和标签超限后，该局面单独复测最终有效且无安全回退。正式部署状态待确认，本轮未推送、未部署。

## 33. 2026-09-02 专业棋手术语平衡

- 根据人工审核意见，将过度口语化的教练表达调整为专业棋手复盘语言；允许使用中心张力、支点、弱格、开放线、交换次序、子力协调、强制变化等术语，但必须同时给出具体棋子、格子、路线或直接后果。
- 专业分析提示词升至 `professional-v28-professional-coach-language`；继续禁止程序报告腔和只有抽象结论的术语堆砌，不降低事实、路线、颜色或引用校验。
- 第五轮人工复审页继续使用原路径 `work/human-review-v25-round3/boards.html`，7个局面已全部改写为“专业术语 + 具体变化”，并修正了把Bb5误称为直接钉住c6马的潜在表述问题。
- 相关专项测试 `99 passed, 2 warnings`；审核页JavaScript语法通过。v28真实15局面最终有效15/15、安全回退0/15、首次通过13/15，另2局因空泛术语被拦截后在第二次生成通过。
- 本轮未推送、未部署；线上状态未改变。

## 34. 2026-09-02 第五轮人工意见裁定

- 读取用户导出的 `human-review-v26-human-coach-results.json`：7局中3局通过、4局需改；裁定记录已保存到 `work/human-review-v25-round3/human-review-v26-professional-terms-adjudication.json`。
- `opening-1`删除“问它”等口语化比喻以及用户明确要求删除的评价免责声明；`opening-3`改为“建立中心张力、挑战e5支点”的专业表达。
- `king-attack-3`删除有争议的叠兵结论，按PV明确写为 `h4 Rd7 hxg5 hxg5`：黑方g5兵被消除、h6兵回吃到g5并打开h线。
- `simplify-2`修正时序：`Qxb5`不是 `...b5` 后立即发生，必须先经过 `g3 Qd7 Nc5 Nxc5 bxc5 Qd5`；后端程序生成的延后吃兵摘要同步加入中间着法。
- 危险事实门禁进一步收紧：程序未确认直接或准备型威胁时，模型不得自行保留最大危险；事实清理导致危险后果为空时重建完整后果句。
- 审核页首次加载本轮版本时会清除上一轮页面选择，新的导出版本和文件名为 `v29-adjudicated`；原导出意见已在项目内留档。
- 相关专项测试 `101 passed, 2 warnings`；三个先前失败的真实局面单独复测均首次通过、最终有效且无安全回退。完整15局已在本轮提示词修改后运行，其余12局通过；最终三个边界修复以针对性真实复测覆盖。
- 本轮未推送、未部署；线上状态未改变。

## 35. 2026-09-02 第五轮人工验收通过

- 已读取用户导出的 `human-review-v29-adjudicated-results.json`；7个审核局面全部通过，需修改0项，没有新增文字意见。
- 最终人工验收记录保存为 `work/human-review-v29-adjudicated/human-review-final-approval.json`。
- 本次通过覆盖开局选择、强制交换、王翼兵形、中心破兵、封闭中心行动次序和简化局面的延后吃兵时序。
- 当前结论仅代表本地第五轮样本通过人工审核；本轮未推送、未部署，线上状态未改变。

## 36. 2026-09-09 v40 已验证棋理命题闭环

- 专业分析提示词与缓存版本升至 `professional-v40-verified-narrative-claims`。后端先用 `python-chess`、Stockfish、威胁包和已验证计划生成原子棋理命题；DeepSeek只选择讲解重点，核心正文最终由程序按“局面线索、实战选择、引擎判词、具体回应、教学原则”重建。
- 核心正文新增 `claimRefs`。程序会拒绝未知命题引用，并在通用文字清理和长度处理后恢复精确命题渲染；正确命题后追加无证据句时，`exactRenderMatch` 与严格落地指标会失败。
- 实战续算、三条候选路线、首着作用、对手第一回应、路线排名、比较结论、`errorType`、`strategyTags` 和对应证据作用域均由后端按已验证路线重建；模型生成的伪因果、跨路线证据和无证据优缺点不会进入最终结果。
- 修正了四个边界：实战续算第0项才是对手第一回应；走后战术不得标为“走棋前”；升变后的控制与攻击必须使用升变棋子名称；计划ID不得混入棋局 `evidenceRefs`。
- 取消为满足字数下限而重复追加同一句事实。已验证命题结果使用更合理的长度区间，保持完整句与证据密度，不用重复文字填充篇幅。
- 最新15局真实Stockfish + DeepSeek套件：首次通过15/15、最终严格通过15/15、安全回退0/15；核心命题精确渲染15/15，72/72条声明落地，未知命题引用0，额外核心文字0，缓存响应5ms。运行环境实际模型为 `deepseek-v4-flash`。
- 人工金标关键原子召回89.7%，其中核心97.8%、细节73.9%、已验证计划94.1%。引用ID合法率与核心命题落地不代表全部页面文字的专家准确率；全表面专家事实率仍需更大规模、人工复核的分层测试集。
- 最新完整pytest为 `371 passed, 3 failed, 3 warnings`；3项失败仍是既有前端页面与断言漂移，新增专业分析测试全部通过；`git diff --check`通过。本轮未提交、未推送、未部署。
- 结果：`work/verified-narrative-v40/results.json`；报告：`work/verified-narrative-v40/report.md`。

## 37. 2026-09-11 Lichess 评注研究库试导入

- 实际检索并下载公开Study `iDPne2Gq` 与 `NulVwm5q`，共8章、592个主线半回合；保存75段去掉引擎标签后至少30字符的文字，包含来源说明，不代表75条已验证棋理。另一个候选 `8IbZHwn7` 返回403，已跳过。
- 新增 `scripts/import_lichess_study.py`：离线导入PGN，逐节点验证正式走法并保存父节点、分支路径、走前/走后FEN、评注和NAG；拒绝无效分支、来源章节不匹配、重复章节和无实质文字的输入。原始下载和哈希保存在 `work/lichess-study-pilot/`。
- 数据进入独立 `work/lichess-study-pilot/research.sqlite3`，2个来源、8条对局记录、8条FTS记录、600个树节点；重复导入数量不变、完整性ok、无外键错误，现有仓库检索与PGN读取可用。本批PGN没有结构化分支，文字中的变化保留原文且未验证；分支保存能力由测试覆盖。
- 原始评注作者、使用许可、文字分析和引擎评价均待核实，记录标为研究用途且不进入正式分析。导入器拒绝写入默认正式数据库。未修改生产分析、提示词、前端或正式数据库，未提交、推送、部署。
- 新增导入测试4项；导入、现有评注和统一棋书检索专项合计14 passed、1个pytest缓存权限警告。完整pytest收集被本轮未修改的 `scripts/run_professional_quality_suite.py:240` 中 `await outside async function` 语法错误阻断，不能宣称全量回归通过；`git diff --check`通过。本轮未运行真实DeepSeek套件。
- 报告与复现命令：`work/lichess-study-pilot/README.md`；统计：`work/lichess-study-pilot/manifest.json`。

## 38. 2026-09-12 v41 强制思考路径与因果边界

- 专业分析提示词升至 `professional-v41-forced-thought-path`。程序强制核心正文按“全局态势 → 实战选择 → 关键比较或转折 → 棋理启示”排列；全局态势固定来自 Stockfish 走前评价，局部棋子事实只能补充，不能替代全局判断。
- 次佳着分析必须同时交代引擎评价差和对手首应。若首应立即吃掉重要棋子，只把它写成已验证线索，并明确现有数据没有证明该次吃子单独造成全部分差；若关键事件出现在路线后段，则明确禁止把后段事件提前写成直接惩罚。
- 修正“没有可用续算”等同“棋局结束”的错误：仅当 `python-chess` 判定走后局面已经结束时才使用终局表述，否则明确说明无法确认最强回应。走法事件校验只检查实战着本身，不再把核心正文中的对手回应误判为实战着吃子。
- 已验证命题结果的普通长度上限调整为 1650 字；最终通用文字清理和长度处理后重新执行程序事实表面重建，避免候选路线、比较结论或核心命题被后处理清空。
- 最新15局真实 Stockfish + DeepSeek 套件：首次通过13/15、最终严格通过15/15、安全回退0/15；核心命题精确渲染15/15、89/89条声明逐字落地；强制思考路径15/15，2个次佳着局面均正确处理惩罚边界，其中1局为首应立即吃子线索、1局保留后段路线边界。运行环境实际模型为 `deepseek-v4-flash`。
- 人工金标关键原子召回89.7%，核心97.8%、细节73.9%、已验证计划94.1%。本套件能够证明这15个样本的程序核心零观测越界，不能据此宣称总体幻觉概率为零，也不能替代更大规模的GM事实盲评和棋书文风盲评。
- 专项测试 `123 passed, 2 warnings`；完整 pytest 为 `381 passed, 3 failed, 3 warnings`，3项失败仍是本轮未修改的前端页面与旧断言漂移。`git diff --check`通过，根目录与发布目录 `index.html` 的SHA-256一致。本轮未提交、未推送、未部署。
- 结果：`work/verified-narrative-v41-final/results.json`；报告：`work/verified-narrative-v41-final/report.md`。

## 39. 2026-09-13 换步结论请求与失败恢复

- 修复前端缓存命中不递增请求令牌的问题：切回已缓存步数后，较早请求不能覆盖当前结论。缓存键改为棋谱分析ID和步数，旧棋谱响应不能写入新棋谱缓存。
- 换步立即清空并隐藏旧摘要；250ms内连续换步仅请求最终选择，同一步尚未完成的请求复用。请求最长等待180秒，失败后状态退出“分析中”，提供重新生成按钮；404缓存过期提供重新评价整盘棋入口。
- 修改 `index.html`、同步 `docs/index.html`，新增 `tests/professional_navigation.test.cjs`。5项异步行为测试全部通过，覆盖缓存切回、快速换步、请求复用、跨棋谱隔离、失败重试和过期恢复。未修改后端、提示词或事实校验。
- 完整pytest修改前381 passed、3 failed，修改后381 passed、3 failed；既有失败为公测文案、棋谱解析器加载方式、开局展示断言。内联JavaScript语法、页面同步、git diff --check通过。Edge无头浏览器在1440/768/390px无横向溢出和JavaScript异常，失败恢复界面已截图检查。
- 真实Render接口4半回合整盘分析12.33秒完成，第1/2步专业结论分别11.89/11.73秒成功返回。通过localhost:8080修复页面连接该线上API，1→2→1换步均显示有效结论。该验证不代表已复现用户截图中的特定局面接口失败；截图对应棋谱和线上异常原因仍待确认。
- 验证产物位于 `work/conclusion-switch-fix/`。本轮未提交、推送或部署，线上前端尚未应用本轮修复。

# 长期开发规则

开始任何工作前，先阅读 `docs/PROJECT_STATUS.md`，再检查当前 `git status`、相关 diff、实现文件和对应测试。状态文档记录当前工作点；本文件只规定长期稳定的协作规则。

## 1. 保持现有功能

- 不得破坏 PGN 解析、棋盘交互、Stockfish、DeepSeek、缓存、API 或部署流程。
- 不得把视觉修改扩大为业务重写，不得在需求之外增加功能。
- DeepSeek 不能成为棋盘事实来源；棋子、格子、走法、评价、PV 和特殊走法必须来自 `python-chess`、Stockfish 或已验证事实包。
- 不得为了提高生成通过率而放松事实、路线、颜色或 evidenceRefs 校验。
- 工作区可能包含用户修改。不得使用 `git reset --hard`、批量删除或覆盖无关文件。

## 2. 修改前检查

- 阅读 `docs/PROJECT_STATUS.md`。
- 运行 `git status --short --branch` 和相关文件的 `git diff`。
- 查找调用链、数据模型、测试和开发版/发布版副本后再编辑。
- 无法从代码、测试、Git 或文档确认的结论必须标为“待确认”，不得根据旧聊天猜测。
- 不读取、打印、复制或提交 `.env`、API Key、Authorization 头或其他密钥。

## 3. 修改后验证

基础测试：

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

还必须按改动范围检查：

- 前端：检查内联 JavaScript 语法、桌面/平板/手机布局、控制台错误和横向溢出。
- 同步：确认根目录 `index.html` 与 `docs/index.html` 内容一致。
- 后端：检查相关 API 测试；必要时启动服务并检查 `/api/health`。
- 专业分析：运行引用、严格校验、重点筛选和安全回退相关测试。
- 涉及提示词、Token、性能或专业内容质量时，运行 15 局面真实质量套件；普通单元测试不能替代真实 DeepSeek 验证。
- 提交前运行 `git diff --check`，并再次检查没有 `.env` 或密钥进入暂存区。

## 4. 前端、后端和部署目录

- `index.html` 是本地开发页面。
- `docs/index.html` 是 GitHub Pages 发布页面；相关静态资源位于 `docs/assets/`。
- 修改前端时同步更新开发版与发布版，不允许只改一处。
- `app/` 是 FastAPI 后端；`Dockerfile` 与 `render.yaml` 是 Render 部署配置。
- `runtime-config.js` 和 `docs/runtime-config.js` 保存公开的后端 origin，不保存密钥。
- 本地必须通过 `http://localhost:8080` 打开网页；不要用 `file:///` 验证后端连接。

## 5. 代码与内容风格

- 在现有结构上做最小、可审查的修改，不重写整个项目。
- Python 保持类型明确、函数职责单一，并继续使用现有 Pydantic 模型。
- 前端延续现有原生 HTML/CSS/JavaScript，不引入新框架，除非用户明确授权。
- 用户可见中文必须自然、完整、具体；禁止显示内部变量名、证据 ID、判断过程或残句。
- 文本清理必须整句保留、整句重建或整句删除，不能删掉部分词后留下语法残片。
- 动态栏目没有有效内容时隐藏，不用“无”强行占位。
- 视觉保持“棋盘研究所”现有配色、圆角、间距和响应式规则，棋盘始终是分析页面核心。

## 6. 交付说明

每次完成修改后，向用户明确说明：

- 修改了哪些文件。
- 实现了什么。
- 运行了哪些测试及结果。
- 哪些内容已经验证，哪些仍待确认。
- 是否发现影响原有功能的问题。

重要里程碑、测试数据、工作停点或部署状态发生变化时，更新 `docs/PROJECT_STATUS.md`；不要把大量项目历史复制到本文件。
